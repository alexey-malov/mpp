# Лабораторная работа №9

- [Лабораторная работа №9](#лабораторная-работа-9)
  - [Критерии оценивания работы](#критерии-оценивания-работы)
  - [Задания](#задания)
    - [Задание1: анализ программы на ассемблере Intel 8080 — **30 баллов**](#задание1-анализ-программы-на-ассемблере-intel-8080--30-баллов)
      - [Ваша задача](#ваша-задача)
    - [Задание 2: программа echo для CP/M — **50 баллов**](#задание-2-программа-echo-для-cpm--50-баллов)
      - [Требования](#требования)
      - [Особенности](#особенности)
      - [Что нужно реализовать](#что-нужно-реализовать)
  - [📄 Шпаргалка: ввод-вывод в CP/M (BDOS)](#-шпаргалка-ввод-вывод-в-cpm-bdos)
    - [Основные функции BDOS](#основные-функции-bdos)
      - [1. Ввод символа с клавиатуры (с эхом)](#1-ввод-символа-с-клавиатуры-с-эхом)
      - [2. Вывод одного символа](#2-вывод-одного-символа)
      - [3. Вывод строки](#3-вывод-строки)
      - [4. Ввод символа без ожидания (опционально)](#4-ввод-символа-без-ожидания-опционально)
      - [5. Ввод строки](#5-ввод-строки)
    - [Полезные ASCII-коды](#полезные-ascii-коды)
    - [Пример: вывод символа](#пример-вывод-символа)
    - [Пример: вывод строки](#пример-вывод-строки)
    - [Важно помнить](#важно-помнить)

## Критерии оценивания работы

- Для получения оценки "удовлетворительно" нужно набрать не менее X баллов.
- На оценку "хорошо" нужно набрать не менее Y баллов.
- Для получения оценки "отлично" нужно набрать не менее Z баллов.

## Задания

### Задание1: анализ программы на ассемблере Intel 8080 — **30 баллов**

Вам дана функция на ассемблере **Intel 8080**. Код функции приведён ниже:

```asm
Fn:
        MOV A, D
        MVI D, 00H

        LXI H, 0000H
        MVI B, 08H

Loop:
        DAD H
        ADD A

        JNC Skip

        DAD D

Skip:
        DCR B
        JNZ Loop

        RET
```

#### Ваша задача

1. Для каждой инструкции написать комментарий, что она делает.

2. Определить:

   - в каких регистрах функция получает входные данные;
   - в каких регистрах возвращает результат;
   - какие регистры изменяются во время работы функции.

3. Отассемблировать программу в машинный код и прогнать её под эмулятором, наблюдая за изменением значений регистров и флагов.

4. Написать эквивалентную программу на языке C.

5. Объяснить словами, что делает эта функция.

---

### Задание 2: программа echo для CP/M — **50 баллов**

Напишите программу на ассемблере Intel 8080, работающую в операционной системе **CP/M**.

Программа должна:

- считывать символ с клавиатуры
- выводить этот же символ на экран
- повторять это в цикле

Программа должна завершить работу, если пользователь нажал клавишу **Enter**.

#### Требования

1. Реализовать цикл обработки ввода:

   - чтение символа
   - проверка условия завершения
   - вывод символа

2. Программа должна корректно завершаться и возвращать управление операционной системе.

3. Программа должна быть оформлена как исполняемый `.COM`-файл (начало с адреса `100h`).

#### Особенности

- клавиша **Enter** соответствует символу с кодом `0Dh`
- этот символ не должен выводиться — программа должна завершиться

#### Что нужно реализовать

Алгоритм программы:

```text
цикл:
    считать символ
    если символ == Enter → завершить программу
    вывести символ
    перейти к началу цикла
```

---

## 📄 Шпаргалка: ввод-вывод в CP/M (BDOS)

Вызов функции операционной системы выполняется так:

```asm
MVI C, <номер функции>
; задать параметры (если нужны)
CALL 5
```

результат (если есть) возвращается обычно в регистре `A`

### Основные функции BDOS

#### 1. Ввод символа с клавиатуры (с эхом)

```asm
MVI C, 1
CALL 5
```

результат:

- символ возвращается в регистре `A`

особенность:

- символ автоматически отображается на экране

#### 2. Вывод одного символа

```asm
MVI C, 2
MOV E, <символ>
CALL 5
```

параметр:

- `E` — символ для вывода

#### 3. Вывод строки

```asm
MVI C, 9
LXI D, <адрес строки>
CALL 5
```

параметры:

- `DE` — адрес строки

важно:

- строка должна заканчиваться символом `$`:

#### 4. Ввод символа без ожидания (опционально)

```asm
MVI C, 6
MVI E, 0FFh
CALL 5
```

результат:

- символ в `A` (если есть)
- иначе `A = 0`

#### 5. Ввод строки

```asm
MVI C, 0Ah
LXI D, <буфер>
CALL 5
```

формат буфера:

```text
[0] — максимальная длина
[1] — фактическая длина
[2..] — введённые символы
```

### Полезные ASCII-коды

| Символ | Код |
| ------ | --- |
| Enter  | 0Dh |
| '0'    | 30h |
| 'A'    | 41h |
| 'a'    | 61h |

### Пример: вывод символа

```asm
MVI C, 2
MVI E, 'A'
CALL 5
```

### Пример: вывод строки

```asm
MVI C, 9
LXI D, MSG
CALL 5

MSG: DB 'HELLO$'
```

### Важно помнить

всегда:

- номер функции записывайте в `C`
- параметры записывайте в другие регистры (зависит от номера функции)

`CALL 5` — точка входа в BDOS
