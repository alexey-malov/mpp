# Лабораторная работа №8

- [Лабораторная работа №8](#лабораторная-работа-8)
  - [Критерии оценивания работы](#критерии-оценивания-работы)
  - [Задания](#задания)
    - [Задание 1: Сортировка строк с использованием `qsort` — **30 баллов**](#задание-1-сортировка-строк-с-использованием-qsort--30-баллов)
      - [Условие](#условие)
      - [Формат входных данных](#формат-входных-данных)
      - [Формат выходных данных](#формат-выходных-данных)
      - [Требования](#требования)
      - [Подсказки](#подсказки)
      - [Пример](#пример)
        - [Ввод:](#ввод)
        - [Вывод:](#вывод)
    - [Задание 2: Полиморфизм на C с использованием указателей на функции — **100 баллов**](#задание-2-полиморфизм-на-c-с-использованием-указателей-на-функции--100-баллов)
      - [Условие](#условие-1)
      - [Поддерживаемые фигуры](#поддерживаемые-фигуры)
        - [Circle](#circle)
        - [Rectangle](#rectangle)
        - [Triangle](#triangle)
      - [Требуемые операции над фигурами](#требуемые-операции-над-фигурами)
      - [Формат входных данных](#формат-входных-данных-1)
      - [Формат выходных данных](#формат-выходных-данных-1)
        - [1. Сначала необходимо отсортировать фигуры по возрастанию площади и вывести их в формате:](#1-сначала-необходимо-отсортировать-фигуры-по-возрастанию-площади-и-вывести-их-в-формате)
        - [2. Затем необходимо отсортировать те же фигуры по убыванию периметра и снова вывести их в том же формате.](#2-затем-необходимо-отсортировать-те-же-фигуры-по-убыванию-периметра-и-снова-вывести-их-в-том-же-формате)
      - [Требования к реализации](#требования-к-реализации)
        - [Архитектура](#архитектура)
        - [Интерфейс модуля](#интерфейс-модуля)
        - [Работа программы](#работа-программы)
      - [Дополнительные требования](#дополнительные-требования)
      - [Подсказки](#подсказки-1)
        - [1. Полиморфизм](#1-полиморфизм)
        - [2. Сортировка](#2-сортировка)
        - [3. Вывод](#3-вывод)
      - [Пример входных данных](#пример-входных-данных)
      - [Пример вывода](#пример-вывода)
      - [Что должно получиться](#что-должно-получиться)
    - [Задание 3: Интерпретатор консольных команд — **70 баллов**](#задание-3-интерпретатор-консольных-команд--70-баллов)
      - [Цель](#цель)
      - [Задание](#задание)
      - [Ключевая идея](#ключевая-идея)
      - [Интерфейс команды](#интерфейс-команды)
      - [Контекст выполнения](#контекст-выполнения)
      - [Обязательные команды](#обязательные-команды)
        - [`help`](#help)
        - [`status`](#status)
        - [`moverobot`](#moverobot)
        - [`exit`](#exit)
      - [Требования к реализации](#требования-к-реализации-1)
      - [Обработка ошибок](#обработка-ошибок)
      - [Пример работы](#пример-работы)
      - [Бонус за реализацию дополнительных команд: **30 баллов**](#бонус-за-реализацию-дополнительных-команд-30-баллов)

## Критерии оценивания работы

- Для получения оценки "удовлетворительно" нужно набрать не менее 100 баллов.
- На оценку "хорошо" нужно набрать не менее 150 баллов.
- Для получения оценки "отлично" нужно набрать не менее 200 баллов.

## Задания

### Задание 1: Сортировка строк с использованием `qsort` — **30 баллов**

#### Условие

Программа считывает из стандартного ввода число `N` — количество строк.

Далее вводятся `N` строк текста.

Необходимо:

1. Прочитать все строки в динамическую память
2. Отсортировать их **по возрастанию (лексикографически)** с использованием функции `qsort`
3. Вывести отсортированные строки в стандартный вывод

#### Формат входных данных

- В первой строке содержится целое число `N` (`N ≥ 0`)
- Далее следуют `N` строк произвольной длины (в пределах разумных ограничений)

#### Формат выходных данных

- Вывести `N` строк в отсортированном порядке, по одной строке на строку

#### Требования

- Использовать функцию `qsort` из стандартной библиотеки
- Реализовать собственную функцию-компаратор
- Хранить строки в виде массива указателей (`char*`)
- Память под строки должна выделяться динамически
- Корректно освободить всю выделенную память

#### Подсказки

- Компаратор должен иметь сигнатуру:

```c
int cmp(const void* a, const void* b);
```

- Элементы массива — это указатели на строки (`char*`), поэтому в компараторе потребуется двойное приведение:

```c
const char* s1 = *(const char**)a;
const char* s2 = *(const char**)b;
```

- Для сравнения строк используйте `strcmp`

#### Пример

##### Ввод:

```txt
4
banana
apple
pear
apricot
```

##### Вывод:

```txt
apple
apricot
banana
pear
```

### Задание 2: Полиморфизм на C с использованием указателей на функции — **100 баллов**

#### Условие

Необходимо реализовать модуль `shapes`, поддерживающий работу с тремя типами фигур:

- `Circle` — окружность
- `Rectangle` — прямоугольник
- `Triangle` — треугольник

Для всех фигур должен использоваться единый интерфейс `Shape`.
Реализация должна быть построена по полиморфной схеме на языке C:
через непрозрачный тип, указатели на функции и таблицу виртуальных функций.

#### Поддерживаемые фигуры

##### Circle

Задаётся:

- координатами центра
- радиусом
- цветом

##### Rectangle

Задаётся:

- координатами левого верхнего угла
- шириной
- высотой
- цветом

##### Triangle

Задаётся:

- координатами трёх вершин
- цветом

#### Требуемые операции над фигурами

Для каждой фигуры должны быть доступны следующие операции:

- `ShapeGetArea` — вычисление площади
- `ShapeGetPerimeter` — вычисление периметра
- `ShapeDraw` — вывод фигуры в текстовом виде

Клиентский код должен работать только с указателями типа `Shape*`, не зная конкретного типа фигуры.

#### Формат входных данных

В первой строке вводится целое число `N` — количество фигур.

В следующих `N` строках записаны описания фигур в одном из следующих форматов:

```text
circle x y radius color
rectangle left top width height color
triangle x1 y1 x2 y2 x3 y3 color
```

Цвет задаётся строкой в формате:

```text
#rrggbb
```

где `rr`, `gg`, `bb` — шестнадцатеричные значения компонент цвета.

#### Формат выходных данных

##### 1. Сначала необходимо отсортировать фигуры по возрастанию площади и вывести их в формате:

```text
circle x y radius color area perimeter
rectangle left top width height color area perimeter
triangle x1 y1 x2 y2 x3 y3 color area perimeter
```

##### 2. Затем необходимо отсортировать те же фигуры по убыванию периметра и снова вывести их в том же формате.

#### Требования к реализации

##### Архитектура

Необходимо реализовать модуль `shapes`, в котором:

- тип `Shape` объявлен как неполный в заголовочном файле
- конкретные структуры фигур скрыты в `.c`-файле
- выбор реализации операций выполняется через таблицу виртуальных функций

##### Интерфейс модуля

В модуле должны быть предусмотрены:

- создание окружности
- создание прямоугольника
- создание треугольника
- уничтожение фигуры
- получение площади
- получение периметра
- вывод фигуры

##### Работа программы

Основная программа должна:

1. прочитать `N`
2. прочитать все фигуры
3. создать массив указателей `Shape*`
4. отсортировать фигуры по площади с помощью `qsort`
5. вывести результат
6. отсортировать фигуры по периметру с помощью `qsort`
7. вывести результат
8. освободить всю выделенную память

#### Дополнительные требования

- Для сортировки использовать `qsort`
- Для вычисления длины стороны треугольника использовать расстояние между точками
- Для вычисления площади треугольника можно использовать формулу Герона или формулу через координаты
- Все объекты должны создаваться динамически
- Вся выделенная память должна быть освобождена
- Необходимо корректно обрабатывать ошибки ввода

#### Подсказки

##### 1. Полиморфизм

Базовая структура `Shape` может хранить указатель на таблицу виртуальных функций.

##### 2. Сортировка

Для `qsort` потребуются компараторы:

- по возрастанию площади
- по убыванию периметра

##### 3. Вывод

Функция `ShapeDraw` должна выводить только данные фигуры,
а площадь и периметр можно выводить отдельно через `ShapeGetArea` и `ShapeGetPerimeter`.

#### Пример входных данных

```text
3
circle 0 0 10 #ff0000
rectangle -1 2 5 3 #00ff00
triangle 0 0 3 0 0 4 #0000ff
```

#### Пример вывода

Численные значения в примере условны.

```text
triangle 0 0 3 0 0 4 #0000ff 6.00 12.00
rectangle -1 2 5 3 #00ff00 15.00 16.00
circle 0 0 10 #ff0000 314.16 62.83

circle 0 0 10 #ff0000 314.16 62.83
rectangle -1 2 5 3 #00ff00 15.00 16.00
triangle 0 0 3 0 0 4 #0000ff 6.00 12.00
```

#### Что должно получиться

В результате должна получиться программа, в которой:

- разные типы фигур обрабатываются через единый интерфейс
- конкретная реализация выбирается во время выполнения
- сортировка выполняется через `qsort` и функции-компараторы
- указатели на функции используются и для полиморфизма, и для сортировки

### Задание 3: Интерпретатор консольных команд — **70 баллов**

#### Цель

Научиться использовать указатели на функции для выбора действия во время выполнения программы,
а также передавать в функции пользовательский контекст через `void*`.

#### Задание

Написать программу-интерпретатор консольных команд.

Программа в цикле считывает строки из `stdin` в формате:

```text
command arg1 arg2 ... argN
```

Затем программа ищет команду в таблице команд и вызывает соответствующую функцию-обработчик.

Работа продолжается до тех пор, пока пользователь не введёт команду:

```text
exit
```

#### Ключевая идея

Каждая команда:

- имеет свою функцию-обработчик
- имеет собственный контекст (void*)

Это позволяет разным командам работать с разными данными.

#### Интерфейс команды

Каждая команда должна описываться структурой:

```c
typedef int (*CommandHandler)(void* context, int argc, char* argv[]);

typedef struct {
    const char* name;
    CommandHandler handler;
    void* context;          // контекст конкретной команды
    const char* help;
} Command;
```

Где:

- `name` — имя команды;
- `handler` — функция-обработчик;
- `help` — краткое описание команды;
- `context` — указатель на данные, с которыми работает команда;
- `argc` — количество аргументов команды;
- `argv` — массив аргументов.

Например, для строки:

```text
moverobot left 10
```

должно получиться:

```c
argc == 2
argv[0] == "left"
argv[1] == "10"
```

---

#### Контекст выполнения

Для примера можно реализовать управление роботом на плоскости.

```c
typedef struct {
    int x;
    int y;
} Robot;
```

Команды должны изменять или выводить состояние этого робота.

---

#### Обязательные команды

##### `help`

Выводит список доступных команд и краткую справку по ним.

Пример:

```text
help
```

---

##### `status`

Выводит текущие координаты робота.

Пример вывода:

```text
Robot position: x=0 y=0
```

---

##### `moverobot`

Перемещает робота в указанном направлении.

Формат:

```text
moverobot direction steps
```

Где `direction` может быть:

```text
left
right
top
bottom
```

Пример:

```text
moverobot right 5
```

Если робот был в точке `(0, 0)`, после команды он должен оказаться в точке `(5, 0)`.

##### `exit`

Завершает работу интерпретатора, после чего происходит выход из программы.

#### Требования к реализации

Программа должна содержать таблицу команд:

```c
Robot robot = {0, 0};

Command commands[] = {
    { "help", HelpCommand, commands, "show available commands" },
    { "status", StatusCommand, &robot, "show robot position" },
    { "moverobot", MoveRobotCommand, &robot, "move robot" },
    { "exit", ExitCommand, NULL, "exit interpreter" }
};
```

Обратите внимание:

- status и moverobot используют один и тот же контекст (robot)
- help может получить доступ ко всей таблице команд
- exit не требует контекста

Вызов команды должен выполняться через указатель на функцию:

```c
commands[i].handler(commands[i].context, argc, argv);
```

Обратите внимание: контекст берётся из самой структуры команды.

Что это даёт:

- каждая команда может работать со своими данными
- команды становятся независимыми
- можно легко добавлять новые команды

Нельзя реализовывать выбор команды через длинную цепочку:

```c
if (...) {
} else if (...) {
} else if (...) {
}
```

Цель задания — использовать именно таблицу команд.

#### Обработка ошибок

Программа должна корректно обрабатывать ситуации:

- неизвестная команда;
- неверное количество аргументов;
- неверное направление движения;
- неверный формат числа шагов;
- пустая строка ввода.

Пример:

```text
unknown command: jump
```

#### Пример работы

```text
> status
Robot position: x=0 y=0

> moverobot right 10
Robot moved

> moverobot top 3
Robot moved

> status
Robot position: x=10 y=3

> help
Available commands:
help       show available commands
status     show robot position
moverobot  move robot: moverobot left|right|top|bottom steps
exit       exit interpreter

> exit
Bye!
```

#### Бонус за реализацию дополнительных команд: **30 баллов**

Добавить команды:

```text
reset
```

сбрасывает позицию робота в `(0, 0)`.

```text
teleport x y
```

перемещает робота в указанную точку.

```text
history
```

выводит последние введённые команды.
