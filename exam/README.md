# Вопросы к экзамену

- [Вопросы к экзамену](#вопросы-к-экзамену)
  - [Устройство микропроцессора](#устройство-микропроцессора)
  - [Ассемблер и машинный код](#ассемблер-и-машинный-код)
  - [Компилятор, линковка и сборка программы](#компилятор-линковка-и-сборка-программы)
  - [Основы языка C](#основы-языка-c)
  - [Ветвление](#ветвление)
  - [Конструкция switch](#конструкция-switch)
  - [Циклы](#циклы)
  - [Массивы в C](#массивы-в-c)
  - [Представление текста в памяти](#представление-текста-в-памяти)
  - [Ввод, вывод, сравнение строк в C](#ввод-вывод-сравнение-строк-в-c)
  - [Указатели и связь с массивами](#указатели-и-связь-с-массивами)
  - [Динамическая память и строки произвольной длины](#динамическая-память-и-строки-произвольной-длины)

## Устройство микропроцессора

- Что такое ISA (Instruction Set Architecture) и какую роль она играет во взаимодействии программы и процессора? ⭐⭐
- Какие основные компоненты входят в состав микропроцессора и за что отвечает каждый из них? ⭐⭐
- Чем регистры отличаются от оперативной памяти? ⭐⭐
- Какую роль выполняет ALU в микропроцессоре? ⭐
- Какую функцию выполняет счётчик команд (PC/RIP)? ⭐
- Чем архитектура фон Неймана отличается от гарвардской архитектуры? ⭐⭐
- Как современные процессоры часто сочетают элементы обеих архитектур? ⭐⭐⭐
- Почему гарвардская архитектура часто используется в микроконтроллерах? ⭐⭐
- В чём различие между программной моделью архитектуры и её аппаратной реализацией? ⭐⭐⭐
- Что такое машинный код с точки зрения процессора? ⭐⭐

## Ассемблер и машинный код

- Почему один и тот же C-код компилируется в разный машинный код для x86-64 и ARM64? ⭐⭐
- Какие преимущества и недостатки имеет переменная длина инструкций в x86-64? ⭐⭐⭐
- Какие преимущества и недостатки имеет фиксированная длина инструкций в ARM64? ⭐⭐⭐
- Почему ассемблерный код непереносим между архитектурами? ⭐⭐
- Что такое ABI и зачем оно нужно? ⭐⭐⭐
- Как по ассемблерному коду можно понять, что происходит вызов функции? ⭐⭐
- Почему ассемблер считается «низкоуровневым» языком? ⭐
- В каких случаях программирование на ассемблере действительно оправдано? ⭐⭐⭐

## Компилятор, линковка и сборка программы

- Какие этапы проходит программа от исходного текста до исполняемого файла? ⭐⭐
- Какую роль выполняет компилятор? ⭐
- Чем объектный файл отличается от исполняемого файла? ⭐⭐
- Что такое внешний символ? ⭐⭐
- Какую задачу решает линкер? ⭐⭐
- Почему вызов puts невозможно полностью обработать только компилятором? ⭐⭐⭐
- Что произойдёт, если линкер не найдёт реализацию внешнего символа? ⭐⭐
- Какую информацию содержит заголовочный файл? ⭐⭐
- Что делает директива #include? ⭐
- Чем отличаются #include <...> и #include "..."? ⭐⭐
- Как стандарт языка C обеспечивает переносимость программ? ⭐⭐⭐

## Основы языка C

- Чем локальные переменные отличаются от глобальных? ⭐⭐
- Почему использование неинициализированных переменных опасно? ⭐⭐
- Что такое неопределённое поведение (undefined behavior), в каких ситуациях возникает в языке C? ⭐⭐
- Для чего рекомендуется объявлять переменные ближе к месту их использования? ⭐⭐
- Как работает целочисленное деление в языке C? ⭐⭐
- Как определяется знак результата операции % в языке C? ⭐⭐
- Для чего scanf требует передачи адресов переменных? ⭐⭐
- Почему getchar возвращает int, а не char? ⭐⭐⭐
- Что такое EOF и зачем он нужен? ⭐⭐
- Почему scanf может зациклиться при неверном вводе? ⭐⭐
- Как корректно обрабатывать ошибки ввода при использовании scanf? ⭐⭐
- Как getchar помогает избежать бесконечного цикла при ошибочном вводе? ⭐⭐

## Ветвление

- Почему программа не всегда должна выполнять инструкции строго последовательно сверху вниз? ⭐
- Как конструкция `if-else` обычно реализуется на уровне ассемблера? ⭐⭐
- Как связаны управляющие конструкции языка C с машинными инструкциями процессора? ⭐⭐
- Чем отличается логика программы на уровне C от её представления на уровне ассемблера? ⭐⭐
- Для каких задач используются вложенные условные операторы? ⭐
- Почему рекомендуется всегда использовать фигурные скобки даже для одной инструкции? ⭐
- Какие преимущества и недостатки даёт правило «любое ненулевое значение считается истиной»? ⭐⭐
- Какую роль играют инструкции `cmp`, условные и безусловные переходы при реализации ветвлений? ⭐⭐
- Почему обе ветви условного оператора обычно сходятся в общий участок кода? ⭐⭐

## Конструкция switch

- В каких ситуациях использование `switch` предпочтительнее длинной цепочки `if-else if`? ⭐
- Почему значения `case` должны быть константами времени компиляции? ⭐⭐
- Почему `switch` не может заменить `if` во всех случаях? ⭐⭐
- Какие ограничения конструкции `switch` связаны с типом проверяемого выражения? ⭐⭐
- Почему `switch` хорошо подходит для реализации меню и конечных автоматов? ⭐⭐
- Что такое fallthrough и для чего его применяют? ⭐⭐
- Когда fallthrough является ошибкой, а когда осознанным приёмом программирования? ⭐⭐
- Какие проблемы поддержки программ может создавать активное использование fallthrough? ⭐⭐⭐
- Почему ограничения конструкции `switch` позволяют компилятору генерировать более эффективный код? ⭐⭐⭐
- Какие способы реализации `switch` может использовать компилятор? ⭐⭐⭐
- Как использование битовых масок позволяет ускорять проверку принадлежности значения множеству констант? ⭐⭐⭐
- Почему подобные оптимизации невозможны при использовании произвольных условий в `if`? ⭐⭐⭐

## Циклы

- В каких ситуациях естественно использовать `while`, а в каких — `for`? ⭐⭐
- Какие преимущества даёт ограниченная область видимости переменной-счётчика в цикле `for`? ⭐⭐
- Какие преимущества и недостатки имеет использование оператора запятая в заголовке цикла `for`? ⭐⭐⭐
- В чём принципиальное отличие `do-while` от `while`? ⭐⭐
- Какие задачи особенно удобно решать при помощи `do-while`? ⭐⭐
- Как различия между `while` и `do-while` отражаются в ассемблерном коде? ⭐⭐⭐
- Для чего используются операторы `break` и `continue` внутри цикла? ⭐⭐
- Какие существуют способы выхода из нескольких вложенных циклов? ⭐⭐
- Почему оператор `goto` традиционно считается потенциально опасным для читаемости программы? ⭐⭐
- В каких ситуациях использование `goto` может сделать программу проще, а не сложнее? ⭐⭐⭐
- Сравните использование `goto` и дополнительных флагов для выхода из вложенных циклов. ⭐⭐⭐

## Массивы в C

- Какие гарантии размещения элементов в памяти дают массивы в отличие от отдельных переменных одного и того же типа? ⭐⭐
- Что происходит при обращении к элементу за границами массива в языке C? ⭐⭐⭐
- Какие преимущества и ограничения имеют массивы фиксированного размера? ⭐⭐
- Какой из операторов цикла является основным инструментом обработки массивов в языке C? ⭐⭐
- Как вычислить размер массива в языке C? Почему этот способ перестаёт работать после передачи массива в функцию? ⭐⭐⭐
- Чем отличается двумерный массив от массива указателей? ⭐⭐
- Как в памяти хранятся элементы двумерного массива? ⭐⭐
- Как компилятор вычисляет адрес элемента `a[i][j]`? ⭐⭐⭐
- Какой из способов обхода двумерного массива (по строкам или по столбцам) обычно работает быстрее и почему? ⭐⭐⭐
- Какие задачи естественно моделировать при помощи многомерных массивов? ⭐⭐

## Представление текста в памяти

- Каким образом текст представляется в в программе на языке C? Приведите пример. ⭐
- Как строка `"Hello"` размещается в памяти в виде последовательности байтов? ⭐
- Сколько байтов требуется для строки `"Hello"` в языке C? ⭐
- Почему процессору без дополнительных соглашений недостаточно просто адреса начала строки? ⭐⭐
- Какие последствия возникают, если массив `char` не содержит завершающего `'\0'`? ⭐⭐
- Что такое escape-последовательность? Для чего она применяется? ⭐
- Почему `'\n'` выглядит как два символа в тексте программы, но обозначает один символ? ⭐
- Какие проблемы могут возникнуть при работе с символами, если тип `char` является знаковым? ⭐⭐
- Как функция, подобная `puts`, определяет длину выводимой строки? ⭐
- Какие недостатки имеет модель строк с завершающим `'\0'` по сравнению с моделью `[длина][данные]`? ⭐⭐⭐

## Ввод, вывод, сравнение строк в C

- Чем `puts` отличается от `printf("%s", s)`? ⭐
- Почему спецификатор `%s` в `printf` требует корректно завершённую строку? ⭐
- Как работает `scanf("%s", s)` при чтении строки? ⭐
- Почему при использовании `scanf("%s", s)` необходимо ограничивать максимальную длину ввода? ⭐⭐
- Почему `scanf("%s", s)` не подходит для чтения строк с пробелами? ⭐
- Чем `fgets` безопаснее `scanf("%s", ...)` при чтении строк? ⭐⭐
- Почему после `fgets` часто требуется удалять символ `'\n'`? ⭐
- Как корректно удалить `'\n'` из строки, считанной через `fgets`? ⭐⭐
- Как скопировать строку в строке C? ⭐⭐
- Почему при копировании строки необходимо скопировать и завершающий `'\0'`? ⭐
- Как корректно сравнить строки на равенство или лексикографически? ⭐
- Чем `strcpy` отличается от `strncpy`? ⭐⭐
- Чем `strcmp` отличается от `strncmp`? ⭐⭐
- В каких случаях полезно сравнивать только первые `n` символов строки? ⭐⭐
- Как работает `strcat` и почему перед её использованием нужно знать размер буфера назначения? ⭐⭐
- Почему `strncat` не освобождает программиста от необходимости контролировать размер буфера? ⭐⭐⭐

## Указатели и связь с массивами

- Что такое указатель и какую информацию он хранит? ⭐
- Как объявить указатель и проинициализировать его адресом некоторой переменной? ⭐
- Как получить доступ к значению, на который указывает указатель? ⭐
- Что происходит при передаче массива в функцию? ⭐⭐
- В чём смысл эквивалентности `s[i]` и `*(s + i)`? ⭐⭐
- Что общего между указателем и массивом и в чём различия между ними? ⭐⭐⭐
- Почему строковые функции обычно получают адрес первого символа, а не сам массив целиком? ⭐⭐
- Чем отличается `const char *p` от `char *const p`? ⭐⭐
- Что запрещает объявление `const char *p`? ⭐
- Что запрещает объявление `char *const p`? ⭐
- Как интерпретировать объявление `const char *const p`? ⭐⭐
- Как константность помогает избежать ошибок при работе со строками? ⭐⭐

## Динамическая память и строки произвольной длины

- Почему статический массив фиксированного размера не всегда подходит для хранения строки? ⭐
- Какие риски возникают при выборе слишком маленького или слишком большого буфера? ⭐⭐
- Как `malloc` позволяет создавать массивы во время выполнения программы? ⭐
- Почему при выделении памяти под копию строки нужно выделять `strlen(s) + 1` байтов? ⭐
- Как функции `malloc` и `realloc` сигнализируют о нехватке памяти? Как корректно обработать эту ситуацию? ⭐
- Как освободить память, выделенную через `malloc` или `realloc`? ⭐
- Чем отличается размещение указателя на стеке от размещения данных в куче? ⭐⭐
- Как работает чтение строки произвольной длины с использованием `getchar` и `realloc`? ⭐⭐⭐
- Почему при использовании `realloc` рекомендуется сохранять результат во временный указатель,
  а не в указатель, переданный в функцию? ⭐⭐⭐
- Как выделить память под массив из `N` элементов типа `int`? ⭐
- Какие обязанности появляются у программиста при использовании динамического массива? ⭐⭐
