- [Основы микропроцессоров и языка C](#основы-микропроцессоров-и-языка-c)
  - [Устройство микропроцессора](#устройство-микропроцессора)
  - [Ассемблер и машинный код](#ассемблер-и-машинный-код)
  - [Компилятор, линковка и сборка программы](#компилятор-линковка-и-сборка-программы)
  - [Основы языка C](#основы-языка-c)
  - [Ветвление](#ветвление)
  - [Конструкция switch](#конструкция-switch)
  - [Циклы](#циклы)
  - [Массивы в C](#массивы-в-c)
  - [Представление текста в памяти](#представление-текста-в-памяти)
  - [Ввод, вывод, сравнение строк в C](#ввод-вывод-сравнение-строк-в-c)
  - [Указатели и связь с массивами](#указатели-и-связь-с-массивами)
  - [Динамическая память и строки произвольной длины](#динамическая-память-и-строки-произвольной-длины)
  - [Структуры в C](#структуры-в-c)
  - [typedef и пользовательские типы](#typedef-и-пользовательские-типы)
  - [Копирование структур и их передача в функцию](#копирование-структур-и-их-передача-в-функцию)
  - [Выравнивание полей в структурах и вопросы производительности](#выравнивание-полей-в-структурах-и-вопросы-производительности)
  - [Память, адреса и система типов](#память-адреса-и-система-типов)
  - [Размеры типов и ограничения представления данных](#размеры-типов-и-ограничения-представления-данных)
  - [Знаковые и беззнаковые числа](#знаковые-и-беззнаковые-числа)
  - [Переполнение целочисленных типов, типы фиксированной разрядности](#переполнение-целочисленных-типов-типы-фиксированной-разрядности)
  - [Представление данных в памяти и порядок байтов, перечислимый тип данных](#представление-данных-в-памяти-и-порядок-байтов-перечислимый-тип-данных)
  - [Битовые операции и работа с битами](#битовые-операции-и-работа-с-битами)
  - [Указатели и адресная арифметика](#указатели-и-адресная-арифметика)
  - [`void *`, `const void *` и побайтовая интерпретация памяти](#void--const-void--и-побайтовая-интерпретация-памяти)
  - [Битовые операции, битовые поля и ручная упаковка данных](#битовые-операции-битовые-поля-и-ручная-упаковка-данных)
  - [`union` и несколько представлений одной области памяти](#union-и-несколько-представлений-одной-области-памяти)
  - [Tagged union и безопасное хранение вариантов данных](#tagged-union-и-безопасное-хранение-вариантов-данных)
  - [Представление дробных чисел: fixed-point, rational и floating point](#представление-дробных-чисел-fixed-point-rational-и-floating-point)
  - [Формат IEEE 754 и битовое представление чисел](#формат-ieee-754-и-битовое-представление-чисел)
  - [Floating point в языке C: типы, ввод-вывод и преобразования](#floating-point-в-языке-c-типы-ввод-вывод-и-преобразования)
  - [Точность, сравнение, epsilon и численная устойчивость](#точность-сравнение-epsilon-и-численная-устойчивость)
  - [Особые значения, `math.h` и математические вычисления](#особые-значения-mathh-и-математические-вычисления)
  - [Аппаратная поддержка чисел с плавающей запятой, embedded-системы и оптимизации](#аппаратная-поддержка-чисел-с-плавающей-запятой-embedded-системы-и-оптимизации)
  - [Файл как внешнее хранилище данных, потоки и стандартный ввод-вывод в C](#файл-как-внешнее-хранилище-данных-потоки-и-стандартный-ввод-вывод-в-c)
  - [Символьный, строковый и форматированный ввод-вывод](#символьный-строковый-и-форматированный-ввод-вывод)
  - [Бинарный ввод-вывод и представление данных, endianness](#бинарный-ввод-вывод-и-представление-данных-endianness)
  - [Позиция в файле и произвольный доступ, буферизация, синхронизация и EOF](#позиция-в-файле-и-произвольный-доступ-буферизация-синхронизация-и-eof)
  - [Организация многофайловой программы и интерфейсы модулей](#организация-многофайловой-программы-и-интерфейсы-модулей)
  - [Сборка, компиляция, компоновка и заголовочные файлы](#сборка-компиляция-компоновка-и-заголовочные-файлы)
  - [Видимость, `static` и инкапсуляция данных](#видимость-static-и-инкапсуляция-данных)
  - [Указатели на функции: объявление, типы и типичные ошибки](#указатели-на-функции-объявление-типы-и-типичные-ошибки)
  - [Компараторы, универсальная сортировка и `qsort`](#компараторы-универсальная-сортировка-и-qsort)
  - [Указатели на функции в структурах и абстракция потока данных](#указатели-на-функции-в-структурах-и-абстракция-потока-данных)
  - [Потоки в памяти, контекст и безопасность работы с буферами](#потоки-в-памяти-контекст-и-безопасность-работы-с-буферами)
  - [Единый интерфейс, opaque type и таблица виртуальных функций](#единый-интерфейс-opaque-type-и-таблица-виртуальных-функций)
  - [Базовая структура, приведение типов, создание и уничтожение объектов](#базовая-структура-приведение-типов-создание-и-уничтожение-объектов)
  - [Организация адресного пространства и областей памяти](#организация-адресного-пространства-и-областей-памяти)
  - [Стек, кадры функций и возврат управления](#стек-кадры-функций-и-возврат-управления)
  - [Соглашения о вызовах, регистры и ассемблерный уровень](#соглашения-о-вызовах-регистры-и-ассемблерный-уровень)
  - [Время жизни объектов и ошибки доступа к памяти](#время-жизни-объектов-и-ошибки-доступа-к-памяти)
  - [Куча, динамическая память и статическое состояние](#куча-динамическая-память-и-статическое-состояние)
  - [Иерархия памяти, кеш и cache line](#иерархия-памяти-кеш-и-cache-line)
  - [Локальность доступа и организация данных](#локальность-доступа-и-организация-данных)
- [Ассемблер](#ассемблер)
  - [Архитектура процессора Intel 8080: машинный код, ISA, модель выполнения, регистры, работа с памятью, флаги](#архитектура-процессора-intel-8080-машинный-код-isa-модель-выполнения-регистры-работа-с-памятью-флаги)
  - [Архитектура процессора Intel 8080: управление выполнением, циклы и ветвления](#архитектура-процессора-intel-8080-управление-выполнением-циклы-и-ветвления)
  - [Архитектура x86-64: регистры общего назначения и специальные регистры](#архитектура-x86-64-регистры-общего-назначения-и-специальные-регистры)
  - [Архитектура x86-64: базовые инструкции и арифметические операции](#архитектура-x86-64-базовые-инструкции-и-арифметические-операции)
  - [Архитектура x86-64: переходы, циклы и управление потоком, управление памятью и адресация](#архитектура-x86-64-переходы-циклы-и-управление-потоком-управление-памятью-и-адресация)
  - [Архитектура x86-64: функции, стек и ABI, косвенные вызовы](#архитектура-x86-64-функции-стек-и-abi-косвенные-вызовы)
  - [Архитектура x86-64: SIMD, SSE и AVX](#архитектура-x86-64-simd-sse-и-avx)
  - [Архитектура x86-64: intrinsics, floating point и ABI](#архитектура-x86-64-intrinsics-floating-point-и-abi)
- [Программирование микроконтроллеров](#программирование-микроконтроллеров)
  - [Программирование микроконтроллеров: встраиваемые системы и микроконтроллеры](#программирование-микроконтроллеров-встраиваемые-системы-и-микроконтроллеры)
  - [Программирование микроконтроллеров: архитектура AVR, организация памяти, система команд](#программирование-микроконтроллеров-архитектура-avr-организация-памяти-система-команд)
  - [Программирование микроконтроллеров: система команд AVR, стартовый код](#программирование-микроконтроллеров-система-команд-avr-стартовый-код)
  - [Программирование микроконтроллеров: аппаратные регистры, volatile и GPIO, таймеры](#программирование-микроконтроллеров-аппаратные-регистры-volatile-и-gpio-таймеры)
  - [Программирование микроконтроллеров: прерывания и обработчики ISR, polling, sleep mode](#программирование-микроконтроллеров-прерывания-и-обработчики-isr-polling-sleep-mode)
  - [Программирование микроконтроллеров: флаги, прерывания, критические секции, состояние гонки и атомарность](#программирование-микроконтроллеров-флаги-прерывания-критические-секции-состояние-гонки-и-атомарность)
  - [Программирование микроконтроллеров: UART, SPI](#программирование-микроконтроллеров-uart-spi)
  - [Программирование микроконтроллеров: I²C, ADC, PWM](#программирование-микроконтроллеров-ic-adc-pwm)

# Основы микропроцессоров и языка C

## Устройство микропроцессора

- Что такое ISA (Instruction Set Architecture) и какую роль она играет во взаимодействии программы и процессора? ⭐⭐
- Какие основные компоненты входят в состав микропроцессора и за что отвечает каждый из них? ⭐⭐
- Чем регистры отличаются от оперативной памяти? ⭐⭐
- Какую роль выполняет ALU в микропроцессоре? ⭐
- Какую функцию выполняет счётчик команд (PC/RIP)? ⭐
- Чем архитектура фон Неймана отличается от гарвардской архитектуры? ⭐⭐
- Как современные процессоры часто сочетают элементы обеих архитектур? ⭐⭐⭐
- Почему гарвардская архитектура часто используется в микроконтроллерах? ⭐⭐
- В чём различие между программной моделью архитектуры и её аппаратной реализацией? ⭐⭐⭐
- Что такое машинный код с точки зрения процессора? ⭐⭐

## Ассемблер и машинный код

- Почему один и тот же C-код компилируется в разный машинный код для x86-64 и ARM64? ⭐⭐
- Какие преимущества и недостатки имеет переменная длина инструкций в x86-64? ⭐⭐⭐
- Какие преимущества и недостатки имеет фиксированная длина инструкций в ARM64? ⭐⭐⭐
- Почему ассемблерный код непереносим между архитектурами? ⭐⭐
- Что такое ABI и зачем оно нужно? ⭐⭐⭐
- Как по ассемблерному коду можно понять, что происходит вызов функции? ⭐⭐
- Почему ассемблер считается «низкоуровневым» языком? ⭐
- В каких случаях программирование на ассемблере действительно оправдано? ⭐⭐⭐

## Компилятор, линковка и сборка программы

- Какие этапы проходит программа от исходного текста до исполняемого файла? ⭐⭐
- Какую роль выполняет компилятор? ⭐
- Чем объектный файл отличается от исполняемого файла? ⭐⭐
- Что такое внешний символ? ⭐⭐
- Какую задачу решает линкер? ⭐⭐
- Почему вызов puts невозможно полностью обработать только компилятором? ⭐⭐⭐
- Что произойдёт, если линкер не найдёт реализацию внешнего символа? ⭐⭐
- Какую информацию содержит заголовочный файл? ⭐⭐
- Что делает директива #include? ⭐
- Чем отличаются #include <...> и #include "..."? ⭐⭐
- Как стандарт языка C обеспечивает переносимость программ? ⭐⭐⭐

## Основы языка C

- Чем локальные переменные отличаются от глобальных? ⭐⭐
- Почему использование неинициализированных переменных опасно? ⭐⭐
- Что такое неопределённое поведение (undefined behavior), в каких ситуациях возникает в языке C? ⭐⭐
- Для чего рекомендуется объявлять переменные ближе к месту их использования? ⭐⭐
- Как работает целочисленное деление в языке C? ⭐⭐
- Как определяется знак результата операции % в языке C? ⭐⭐
- Для чего scanf требует передачи адресов переменных? ⭐⭐
- Почему getchar возвращает int, а не char? ⭐⭐⭐
- Что такое EOF и зачем он нужен? ⭐⭐
- Почему scanf может зациклиться при неверном вводе? ⭐⭐
- Как корректно обрабатывать ошибки ввода при использовании scanf? ⭐⭐
- Как getchar помогает избежать бесконечного цикла при ошибочном вводе? ⭐⭐

## Ветвление

- Почему программа не всегда должна выполнять инструкции строго последовательно сверху вниз? ⭐
- Как конструкция `if-else` обычно реализуется на уровне ассемблера? ⭐⭐
- Как связаны управляющие конструкции языка C с машинными инструкциями процессора? ⭐⭐
- Чем отличается логика программы на уровне C от её представления на уровне ассемблера? ⭐⭐
- Для каких задач используются вложенные условные операторы? ⭐
- Почему рекомендуется всегда использовать фигурные скобки даже для одной инструкции? ⭐
- Какие преимущества и недостатки даёт правило «любое ненулевое значение считается истиной»? ⭐⭐
- Какую роль играют инструкции `cmp`, условные и безусловные переходы при реализации ветвлений? ⭐⭐
- Почему обе ветви условного оператора обычно сходятся в общий участок кода? ⭐⭐

## Конструкция switch

- В каких ситуациях использование `switch` предпочтительнее длинной цепочки `if-else if`? ⭐
- Почему значения `case` должны быть константами времени компиляции? ⭐⭐
- Почему `switch` не может заменить `if` во всех случаях? ⭐⭐
- Какие ограничения конструкции `switch` связаны с типом проверяемого выражения? ⭐⭐
- Почему `switch` хорошо подходит для реализации меню и конечных автоматов? ⭐⭐
- Что такое fallthrough и для чего его применяют? ⭐⭐
- Когда fallthrough является ошибкой, а когда осознанным приёмом программирования? ⭐⭐
- Какие проблемы поддержки программ может создавать активное использование fallthrough? ⭐⭐⭐
- Почему ограничения конструкции `switch` позволяют компилятору генерировать более эффективный код? ⭐⭐⭐
- Какие способы реализации `switch` может использовать компилятор? ⭐⭐⭐
- Как использование битовых масок позволяет ускорять проверку принадлежности значения множеству констант? ⭐⭐⭐
- Почему подобные оптимизации невозможны при использовании произвольных условий в `if`? ⭐⭐⭐

## Циклы

- В каких ситуациях естественно использовать `while`, а в каких — `for`? ⭐⭐
- Какие преимущества даёт ограниченная область видимости переменной-счётчика в цикле `for`? ⭐⭐
- Какие преимущества и недостатки имеет использование оператора запятая в заголовке цикла `for`? ⭐⭐⭐
- В чём принципиальное отличие `do-while` от `while`? ⭐⭐
- Какие задачи особенно удобно решать при помощи `do-while`? ⭐⭐
- Как различия между `while` и `do-while` отражаются в ассемблерном коде? ⭐⭐⭐
- Для чего используются операторы `break` и `continue` внутри цикла? ⭐⭐
- Какие существуют способы выхода из нескольких вложенных циклов? ⭐⭐
- Почему оператор `goto` традиционно считается потенциально опасным для читаемости программы? ⭐⭐
- В каких ситуациях использование `goto` может сделать программу проще, а не сложнее? ⭐⭐⭐
- Сравните использование `goto` и дополнительных флагов для выхода из вложенных циклов. ⭐⭐⭐

## Массивы в C

- Какие гарантии размещения элементов в памяти дают массивы в отличие от отдельных переменных одного и того же типа? ⭐⭐
- Что происходит при обращении к элементу за границами массива в языке C? ⭐⭐⭐
- Какие преимущества и ограничения имеют массивы фиксированного размера? ⭐⭐
- Какой из операторов цикла является основным инструментом обработки массивов в языке C? ⭐⭐
- Как вычислить размер массива в языке C? Почему этот способ перестаёт работать после передачи массива в функцию? ⭐⭐⭐
- Чем отличается двумерный массив от массива указателей? ⭐⭐
- Как в памяти хранятся элементы двумерного массива? ⭐⭐
- Как компилятор вычисляет адрес элемента `a[i][j]`? ⭐⭐⭐
- Какой из способов обхода двумерного массива (по строкам или по столбцам) обычно работает быстрее и почему? ⭐⭐⭐
- Какие задачи естественно моделировать при помощи многомерных массивов? ⭐⭐

## Представление текста в памяти

- Каким образом текст представляется в в программе на языке C? Приведите пример. ⭐
- Как строка `"Hello"` размещается в памяти в виде последовательности байтов? ⭐
- Сколько байтов требуется для строки `"Hello"` в языке C? ⭐
- Почему процессору без дополнительных соглашений недостаточно просто адреса начала строки? ⭐⭐
- Какие последствия возникают, если массив `char` не содержит завершающего `'\0'`? ⭐⭐
- Что такое escape-последовательность? Для чего она применяется? ⭐
- Почему `'\n'` выглядит как два символа в тексте программы, но обозначает один символ? ⭐
- Какие проблемы могут возникнуть при работе с символами, если тип `char` является знаковым? ⭐⭐
- Как функция, подобная `puts`, определяет длину выводимой строки? ⭐
- Какие недостатки имеет модель строк с завершающим `'\0'` по сравнению с моделью `[длина][данные]`? ⭐⭐⭐

## Ввод, вывод, сравнение строк в C

- Чем `puts` отличается от `printf("%s", s)`? ⭐
- Почему спецификатор `%s` в `printf` требует корректно завершённую строку? ⭐
- Как работает `scanf("%s", s)` при чтении строки? ⭐
- Почему при использовании `scanf("%s", s)` необходимо ограничивать максимальную длину ввода? ⭐⭐
- Почему `scanf("%s", s)` не подходит для чтения строк с пробелами? ⭐
- Чем `fgets` безопаснее `scanf("%s", ...)` при чтении строк? ⭐⭐
- Почему после `fgets` часто требуется удалять символ `'\n'`? ⭐
- Как корректно удалить `'\n'` из строки, считанной через `fgets`? ⭐⭐
- Как скопировать строку в строке C? ⭐⭐
- Почему при копировании строки необходимо скопировать и завершающий `'\0'`? ⭐
- Как корректно сравнить строки на равенство или лексикографически? ⭐
- Чем `strcpy` отличается от `strncpy`? ⭐⭐
- Чем `strcmp` отличается от `strncmp`? ⭐⭐
- В каких случаях полезно сравнивать только первые `n` символов строки? ⭐⭐
- Как работает `strcat` и почему перед её использованием нужно знать размер буфера назначения? ⭐⭐
- Почему `strncat` не освобождает программиста от необходимости контролировать размер буфера? ⭐⭐⭐

## Указатели и связь с массивами

- Что такое указатель и какую информацию он хранит? ⭐
- Как объявить указатель и проинициализировать его адресом некоторой переменной? ⭐
- Как получить доступ к значению, на который указывает указатель? ⭐
- Что происходит при передаче массива в функцию? ⭐⭐
- В чём смысл эквивалентности `s[i]` и `*(s + i)`? ⭐⭐
- Что общего между указателем и массивом и в чём различия между ними? ⭐⭐⭐
- Почему строковые функции обычно получают адрес первого символа, а не сам массив целиком? ⭐⭐
- Чем отличается `const char *p` от `char *const p`? ⭐⭐
- Что запрещает объявление `const char *p`? ⭐
- Что запрещает объявление `char *const p`? ⭐
- Как интерпретировать объявление `const char *const p`? ⭐⭐
- Как константность помогает избежать ошибок при работе со строками? ⭐⭐

## Динамическая память и строки произвольной длины

- Почему статический массив фиксированного размера не всегда подходит для хранения строки? ⭐
- Какие риски возникают при выборе слишком маленького или слишком большого буфера? ⭐⭐
- Как `malloc` позволяет создавать массивы во время выполнения программы? ⭐
- Почему при выделении памяти под копию строки нужно выделять `strlen(s) + 1` байтов? ⭐
- Как функции `malloc` и `realloc` сигнализируют о нехватке памяти? Как корректно обработать эту ситуацию? ⭐
- Как освободить память, выделенную через `malloc` или `realloc`? ⭐
- Чем отличается размещение указателя на стеке от размещения данных в куче? ⭐⭐
- Как работает чтение строки произвольной длины с использованием `getchar` и `realloc`? ⭐⭐⭐
- Почему при использовании `realloc` рекомендуется сохранять результат во временный указатель,
  а не в указатель, переданный в функцию? ⭐⭐⭐
- Как выделить память под массив из `N` элементов типа `int`? ⭐
- Какие обязанности появляются у программиста при использовании динамического массива? ⭐⭐

## Структуры в C

- Какие проблемы возникают при передаче связанных данных через несколько независимых параметров функций? ⭐⭐
- Какие недостатки проявляются при добавлении новых свойств к объекту, если данные организованы в виде параллельных массивов? ⭐⭐
- В чём заключается основная идея структур как механизма моделирования предметной области? ⭐⭐
- Какие преимущества даёт наличие собственного типа данных `Point`
  по сравнению с использованием двух переменных типа `int`? ⭐⭐
- Что такое структура (`struct`) в языке C? ⭐
- Чем структура принципиально отличается от массива? ⭐⭐
- Как поля структуры располагаются в памяти? ⭐⭐⭐
- Каким образом компилятор реализует обращения к полям структуры в архитектуре x86-64? ⭐⭐
- Как объявляется структура в языке C? ⭐
- Как осуществляется доступ к полям структуры, когда есть переменная-значение и переменная-указатель? ⭐
- Приведите примеры использования вложенных структур? ⭐⭐
- Как выполняется обычная инициализация структуры при объявлении переменной? ⭐
- Как выполняется инициализация вложенных структур? ⭐⭐
- Как выполняется инициализация массива, находящегося внутри структуры? ⭐⭐
- Что означает запись `{0}` при инициализации структуры? ⭐⭐
- Как работает частичная инициализация структуры? ⭐⭐
- Что такое designated initializers и какие задачи они решают? ⭐⭐
- Какие преимущества и недостатки имеет инициализация полей по именам? ⭐⭐
- Можно ли при инициализации структуры вызовами функций полагаться на порядок вычисления выражений? ⭐⭐⭐
- Какие ограничения существуют на использование неконстантных выражений при инициализации глобальных структур? ⭐⭐⭐

## typedef и пользовательские типы

- Какие проблемы моделирования данных решают структуры в языке C? ⭐⭐
- Почему появление структур можно считать переходом от работы с отдельными значениями
  к работе с объектами предметной области? ⭐⭐
- Как структуры помогают сделать программу более понятной, надёжной и расширяемой? ⭐⭐⭐
- Какие возможности языка C становятся доступны после освоения структур,
  которые трудно реализовать только с помощью скалярных типов и массивов? ⭐⭐⭐
- Почему структуры являются одним из важнейших механизмов абстракции в языке C? ⭐⭐⭐
- Почему в языке C необходимо писать `struct Point`, а не просто `Point`? ⭐⭐
- Что такое тег структуры? ⭐⭐
- Какую проблему решает оператор `typedef`? ⭐⭐
- Чем отличается создание структуры от создания синонима типа через `typedef`? ⭐⭐
- Какие преимущества даёт использование логических имён типов вроде `Coord` или `Health`? ⭐⭐
- Почему конструкция

```c
typedef struct {
    int x;
    int y;
} Point;
```

часто считается более удобной, чем использование `struct Point`? ⭐

## Копирование структур и их передача в функцию

- Что происходит при присваивании одной структуры другой? ⭐⭐
- Что происходит при копировании структуры, содержащей массивы? ⭐⭐
- Пусть `p1` и `p2` - экземпляры структуры `Point`.
  Как повлияет изменение переменной `p2` после выполнения присваивания `p2 = p1`? ⭐⭐
- Как изменение параметра структурного типа внутри функции влияет на исходный объект? ⭐⭐
- Какие преимущества даёт возможность возвращать структуры из функций по значению? ⭐⭐
- В каких случаях передачу структуры в функцию по значению можно считать разумным решением? ⭐⭐
- В каких случаях лучше передавать структуру через указатель `const Type*`, а в каких — через `Type*`? ⭐⭐
- Почему размер структуры может отличаться от простой суммы размеров её полей? ⭐⭐
- Как можно уменьшить размер структуры в памяти, не меняя состав и типы её полей? ⭐⭐
- Какие факторы следует учитывать при выборе между передачей структуры по значению и по указателю? ⭐⭐⭐
- Может ли использование структур само по себе приводить к снижению производительности программы? Обоснуйте ответ. ⭐⭐⭐

## Выравнивание полей в структурах и вопросы производительности

- Что такое выравнивание данных и зачем оно нужно процессору? ⭐⭐
- Какие последствия может иметь доступ к данным по невыровненному адресу? ⭐⭐
- Что такое padding внутри структуры и почему компилятор добавляет padding-байты между полями структуры или в её конец? ⭐⭐
- Почему компилятор может добавлять padding в конец структуры? ⭐⭐⭐
- Почему структура

```c
struct {
    int age;
    char grade;
};
```

может занимать 8 байт, а не 5? ⭐⭐

- Как порядок объявления полей влияет на размер структуры? ⭐⭐
- Почему перестановка полей иногда позволяет существенно уменьшить объём занимаемой памяти? ⭐⭐
- По какому принципу обычно рекомендуется располагать поля структуры для уменьшения количества padding? ⭐⭐
- Что показывают `sizeof`, `alignof` и `offsetof` и чем они отличаются друг от друга? ⭐⭐⭐
- Как с помощью `offsetof` можно исследовать реальное расположение полей в памяти? ⭐⭐⭐
- Какие оптимизации использует компилятор при передаче небольших структур в функцию на современных архитектурах? ⭐⭐⭐
- Как ABI влияет на способ передачи и возврата структур? ⭐⭐⭐
- Каким образом наличие структуры может помочь компилятору сгенерировать более эффективный код
  по сравнению с отдельными переменными? ⭐⭐⭐
- Почему нельзя заранее утверждать, что версия программы без структур будет быстрее версии со структурами? ⭐⭐⭐
- Почему при оценке эффективности структур важно анализировать реальный машинный код и особенности целевой архитектуры? ⭐⭐⭐

## Память, адреса и система типов

- Почему память компьютера можно рассматривать как массив байтов, и какие ограничения имеет такая модель? ⭐⭐
- Какую роль играют адреса памяти при выполнении программы? ⭐
- Чем регистры процессора отличаются от оперативной памяти с точки зрения архитектуры и производительности? ⭐⭐
- Почему современные языки программирования скрывают от программиста необходимость вручную выбирать адреса памяти? ⭐⭐
- Какие задачи решает система типов в языке C? ⭐⭐
- Почему одни и те же байты могут интерпретироваться программой по-разному? Приведите примеры. ⭐⭐
- В каких случаях компилятор может хранить переменную в регистре вместо оперативной памяти? ⭐⭐⭐

## Размеры типов и ограничения представления данных

- Почему множество целых чисел в математике и множество значений типа int в программе принципиально различаются? ⭐⭐
- Докажите, что число различных значений, которые можно представить в n битах, равно 2ⁿ. ⭐⭐
- Почему архитектура процессора приводит к появлению стандартных размеров данных (8, 16, 32, 64 бита)? ⭐⭐
- Как ширина регистров процессора влияет на эффективность вычислений? ⭐⭐⭐
- Как ширина шины данных влияет на работу с памятью и выбор типов данных? ⭐⭐⭐
- Почему стандарт C не фиксирует точный размер типов int и long? ⭐⭐
- Какие гарантии относительно размеров целочисленных типов предоставляет стандарт языка C? ⭐⭐⭐
- В каких ситуациях использование меньшего целочисленного типа позволяет повысить производительность программы? ⭐⭐

## Знаковые и беззнаковые числа

- Чем отличаются signed и unsigned типы не только по диапазону значений, но и по смыслу использования? ⭐⭐
- Почему беззнаковый тип того же размера может хранить больше положительных чисел, чем знаковый? ⭐
- Для каких задач использование unsigned является наиболее естественным выбором? ⭐⭐
- Как тип char выделяется среди остальных целочисленных типов языка C? ⭐⭐⭐
- Какие проблемы могут возникнуть при смешивании signed и unsigned значений в одном выражении? ⭐⭐
- Какие недостатки имеет представление чисел в формате sign-magnitude? ⭐⭐
- Почему наличие двух представлений нуля считается серьёзной проблемой для архитектуры компьютера? ⭐⭐⭐
- Чем обратный код (one's complement) лучше и хуже представления sign-magnitude? ⭐⭐
- Объясните принцип получения отрицательного числа в дополнительном коде (two's complement). ⭐⭐
- Почему дополнительный код позволяет упростить реализацию арифметики в процессоре? ⭐⭐
- Почему диапазон знаковых чисел в дополнительном коде несимметричен относительно нуля? ⭐⭐
- Покажите, как представить число −42 в виде 8-битного числа в дополнительном коде. ⭐⭐
- Почему большинство современных процессоров используют именно дополнительный код? ⭐⭐

## Переполнение целочисленных типов, типы фиксированной разрядности

- Что такое переполнение целочисленного типа и почему оно возникает? ⭐⭐
- Как определяется переполнение для знаковых и беззнаковых целых типов в стандарте языка C? ⭐⭐⭐
- Какие оптимизации компилятор может выполнять, опираясь на предположение об отсутствии переполнения чисел со знаком? ⭐⭐⭐
- Почему опасно полагаться на конкретный результат переполнения signed int в переносимой программе? ⭐⭐
- Для каких задач обычных типов int и long недостаточно? ⭐⭐
- В чём отличие между типами int32_t и int_least32_t? ⭐⭐⭐
- В каких случаях следует использовать intptr_t и uintptr_t? ⭐⭐⭐
- Почему типы из stdint.h особенно важны в embedded-разработке? ⭐⭐
- Какие проблемы могут возникнуть при чтении бинарного файла, если использовать int вместо int32_t? ⭐⭐⭐

## Представление данных в памяти и порядок байтов, перечислимый тип данных

- Почему вопрос порядка байтов возникает только для многобайтовых типов данных? ⭐
- Объясните различие между little-endian и big-endian представлением данных. ⭐⭐
- Как будет храниться число 0x12345678 в памяти на little-endian и big-endian системах? ⭐
- Почему большинство современных настольных компьютеров используют little-endian порядок байтов? ⭐⭐
- В каких ситуациях программа обязана учитывать порядок байтов, а в каких нет? ⭐⭐
- Что такое network byte order и почему он был стандартизован? ⭐⭐
- Какие проблемы могут возникнуть при обмене бинарными данными между системами с разным endianness? ⭐⭐
- Каким способом программа может определить порядок байтов на текущей платформе? ⭐⭐
- Какие проблемы возникают при использовании «магических чисел» вместо перечислимых типов? ⭐⭐
- Почему enum повышает читаемость и сопровождаемость программы? ⭐⭐
- Как компилятор представляет значения enum в памяти? ⭐⭐
- В каких случаях имеет смысл явно задавать числовые значения элементов перечисления? ⭐⭐
- Почему enum особенно удобен при реализации конечных автоматов и машин состояний? ⭐⭐⭐
- Какие преимущества даёт использование typedef вместе с enum? ⭐

## Битовые операции и работа с битами

- Почему число иногда удобно рассматривать не как значение, а как набор отдельных битов? ⭐⭐
- Что такое битовая маска и какие задачи она позволяет решать? ⭐⭐
- Объясните принцип работы побитовой операции AND и приведите примеры её практического применения. ⭐⭐
- Чем отличаются логические операции && и || от побитовых операций & и |? ⭐⭐⭐
- В каких ситуациях удобно использовать операцию XOR? ⭐⭐
- Почему операция XOR часто применяется в криптографии и алгоритмах обработки данных? ⭐⭐⭐
- Как с помощью побитовых операций проверить, установлен ли конкретный бит числа? ⭐⭐
- Как установить, сбросить и переключить отдельный бит числа? ⭐⭐
- Почему при работе со сдвигами часто рекомендуется использовать unsigned типы? ⭐⭐⭐
- Как связаны операции сдвига и умножение/деление на степени двойки? ⭐⭐
- Какие ограничения и опасности существуют при выполнении побитовых операций над знаковыми числами? ⭐⭐⭐
- Почему упаковка нескольких небольших значений в один байт позволяет экономить память? ⭐⭐
- Какие компромиссы возникают между экономией памяти и сложностью обработки данных? ⭐⭐⭐
- Объясните принцип упаковки двух четырёхбитных значений в один байт. ⭐⭐
- В каких практических задачах используется плотное битовое представление данных? ⭐⭐⭐

## Указатели и адресная арифметика

- Что хранит переменная-указатель в языке C и чем значение указателя отличается от значения объекта, на который он указывает? ⭐
- Почему арифметика указателей в C выполняется в элементах, а не в байтах? Приведите пример для указателей типов `char*`, `int*` и `double*`. ⭐⭐
- Что означает выражение `p + n`, если `p` имеет некий тип `T*`? Как изменится фактический адрес в памяти? ⭐
- При каких условиях стандарт C разрешает выполнять арифметику указателей?
  Какие операции над указателями разрешены, а какие нет? ⭐⭐
- Что означает указатель на элемент «сразу после конца массива»? Какие есть ограничения при работе с ним? ⭐⭐
- Чем отличаются выражения `p + 1` и `(char*)p + 1`, если `p` имеет тип `int*`? ⭐⭐
- Что возвращает выражение `p1 - p2` для двух указателей? Какой тип имеет результат? ⭐
- Почему результат вычитания двух указателей определён только для элементов одного массива?
  Какие ошибки могут возникнуть при нарушении этого правила? ⭐⭐⭐
- Объясните, почему выражение `arr[i]` в языке C эквивалентно `*(arr + i)`. ⭐
- Почему в C выражения `p[i]` и `i[p]` формально эквивалентны (где `p` — указатель, а `i` — число)?
  Почему в реальном коде почти всегда используют только первую форму? ⭐⭐
- Как связаны имя массива и указатель на его первый элемент? Всегда ли массив «превращается» в указатель? ⭐⭐
- Чем отличаются `arr`, `&arr[0]` и `&arr` для массива `int arr[5]`? ⭐⭐⭐
- Что произойдёт при обращении к `p[-1]`? В каких случаях такое выражение может быть корректным,
  а в каких приводит к неопределённому поведению? ⭐⭐
- Почему выражение `sizeof(arr)` и `sizeof(p)` дают разные результаты, если `arr` — массив, а `p` — указатель? ⭐⭐
- Как с помощью двух указателей `begin` и `end` можно задать диапазон элементов массива?
  Почему часто используется полуинтервал `[begin, end)`? ⭐⭐
- Напишите функцию, которая суммирует элементы двух массивов, последовательно увеличивая три указателя:
  на первый массив, второй массив и результат. ⭐⭐
- Почему сравнение указателей с помощью `<`, `<=`, `>` и `>=` корректно только внутри одного массива? ⭐⭐⭐
- Какие ошибки могут возникнуть при выходе указателя за границы массива даже без немедленного разыменования? ⭐⭐⭐
- Почему для вывода значения указателя в `printf` используется спецификатор `%p` и зачем указатель приводят к `void*`? ⭐
- Можно ли делать вывод о физическом расположении объектов в памяти только по значениям указателей,
  напечатанным через `%p`? Почему? ⭐⭐

## `void *`, `const void *` и побайтовая интерпретация памяти

- Что такое `void*` и почему его называют универсальным указателем? ⭐
- Почему через `void*` нельзя напрямую выполнять разыменование, индексирование и арифметику указателей? ⭐⭐
- Зачем перед использованием `void*` обычно приводят к указателю на конкретный тип?
  Какие риски связаны с неправильным приведением? ⭐⭐
- Чем отличается `void*` от `const void*`? Какую защиту даёт `const` в интерфейсах функций? ⭐
- Почему функции вроде `malloc`, `qsort` и пользовательские обобщённые функции часто используют `void*`? ⭐⭐
- Почему через `unsigned char*` или `uint8_t*` удобно рассматривать объект как последовательность байтов?
  Всегда ли `uint8_t` гарантированно существует? ⭐⭐⭐
- Что можно узнать из дампа памяти объекта типа `int`, `double` или строки?
  Какие выводы при этом могут быть непереносимыми? ⭐⭐
- Как порядок байтов little-endian и big-endian влияет на результат побайтового вывода числа `0x12345678`? ⭐⭐
- Почему чтение байтового представления объекта допустимо,
  а чтение того же объекта через указатель на несовместимый тип — нет? ⭐⭐⭐
- Что такое правило strict aliasing в C и зачем оно нужно компилятору? ⭐⭐⭐
- Через какие типы стандарт C разрешает читать объект без нарушения правил алиасинга? ⭐⭐⭐
- Почему преобразование `uint64_t*` к `uint32_t*` и чтение через полученный указатель
  не является переносимым способом интерпретации памяти? ⭐⭐⭐
- Чем отличается чтение байтового представления объекта от интерпретации этих байтов как объекта другого типа? ⭐⭐⭐
- Как нарушение strict aliasing может повлиять на оптимизации компилятора и привести к неожиданному поведению программы? ⭐⭐⭐
- Почему приведение указателей не меняет сами байты в памяти, а только меняет способ их интерпретации компилятором? ⭐⭐
- В каких случаях для преобразования представления данных безопаснее использовать `memcpy`,
  чем приведение указателей или `union`? ⭐⭐⭐
- Как связаны тип объекта, его выравнивание и корректность доступа через указатель?
  Что может произойти при обращении по неправильно выровненному адресу? ⭐⭐⭐

## Битовые операции, битовые поля и ручная упаковка данных

- Зачем может понадобиться хранить несколько небольших значений внутри одного байта или машинного слова? ⭐
- Как с помощью маски `0b11` оставить только два младших бита числа? ⭐
- Объясните последовательность действий при записи двухбитного поля `mode` в младшие биты байта: ограничение значения, 
  очистка поля, запись нового значения. ⭐⭐
- Зачем перед записью значения в биты числа обычно сначала очищают нужные биты
  с помощью операции `&` и инверсной маски? ⭐⭐
- Чем отличаются операции `&`, `|`, `~`, `<<` и `>>` при манипулировании отдельными битами? ⭐
- Почему ручная работа с масками и сдвигами часто предпочтительнее битовых полей
  при описании сетевых протоколов и бинарных файловых форматов? ⭐⭐
- Как можно извлечь из байта поле шириной 5 бит, расположенное начиная с бита номер 2?
  Опишите общий алгоритм и напишите программу на C. ⭐⭐
- Какие ошибки часто возникают при ручной упаковке битов:
  неверная маска, неверный сдвиг, потеря старших битов, знаковое расширение? ⭐⭐
- Что такое битовое поле в структуре C и как задаётся его ширина? ⭐
- Какие типы допустимы для битовых полей? ⭐
- Чем структура с битовыми полями отличается от структуры с обычными полями типа `unsigned char` или `bool`? ⭐⭐
- Почему структура, суммарная ширина битовых полей которой меньше 8 бит, всё равно может занимать 4 байта? ⭐⭐
- Как базовый тип битового поля, например `unsigned` или `unsigned char`, может повлиять на размер структуры? ⭐⭐
- Какие аспекты размещения битовых полей в памяти не гарантируются стандартом C? ⭐⭐⭐
- Почему порядок размещения битовых полей внутри байта или машинного слова зависит от реализации? ⭐⭐⭐
- Что такое padding в структуре с битовыми полями и зачем компилятор может его добавлять? ⭐⭐
- Почему битовые поля не следует использовать для переносимого описания бинарных форматов,
  сетевых пакетов и файловых заголовков? ⭐⭐
- Как компилятор обычно реализует присваивание значения битовому полю на уровне машинного кода? ⭐⭐
- Почему битовые поля можно рассматривать как синтаксически удобную форму операций с масками и сдвигами? ⭐⭐
- Что произойдёт, если присвоить битовому полю значение, которое не помещается в заданное число бит?
  От чего может зависеть результат? ⭐⭐⭐
- Что такое безымянное битовое поле нулевой ширины и как оно влияет на размещение следующих полей? ⭐⭐
- В каких случаях безымянные битовые поля могут быть полезны при описании групп флагов или аппаратных регистров? ⭐⭐
- Почему битовые поля иногда применяются в embedded-системах для описания аппаратных регистров?
  В чём риск такого подхода? ⭐⭐⭐
- Зачем при работе с memory-mapped registers часто используют `volatile`?
  Почему одного `volatile` недостаточно для переносимости битовых полей? ⭐⭐⭐
- Какие компромиссы возникают при использовании битовых полей? ⭐⭐
- Почему в больших массивах структур уменьшение размера структуры за счёт битовых полей может ускорить программу? ⭐⭐

## `union` и несколько представлений одной области памяти

- Что такое `union` в языке C и чем он принципиально отличается от `struct`? ⭐
- Почему все поля `union` имеют один и тот же начальный адрес? ⭐
- Как определяется размер `union`? Почему он равен размеру самого большого поля,
  но может включать дополнительное выравнивание? ⭐⭐
- Как `union` позволяет интерпретировать одну область памяти разными способами? ⭐⭐
- Приведите пример ситуации, когда удобно иметь доступ к одной точке как через поля `x`, `y`, так и через массив `v[2]`. ⭐⭐
- Почему изменение `p.v[0]` в объединении с представлениями `struct { int x; int y; }` и `int v[2]`
  может изменить значение `p.x`? ⭐⭐
- Какие ограничения и риски есть у анонимных структур и объединений в C?
  Насколько такой код переносим между компиляторами и стандартами? ⭐⭐⭐
- Как `union` можно использовать для интерпретации `uint64_t` как массива из двух `uint32_t`?
  Почему результат зависит от endianess? ⭐⭐
- Что такое little-endian и big-endian?
  Как они влияют на порядок элементов `u32[0]` и `u32[1]` при просмотре `uint64_t` через `union`? ⭐⭐
- Что такое type punning и почему эта техника требует осторожности? ⭐⭐
- Чем type punning через `union` отличается от type punning через приведение указателей? ⭐⭐⭐
- Какие свойства программы могут стать непереносимыми при использовании `union` для интерпретации данных? ⭐⭐⭐
- Почему в каждый момент времени обычно корректно читать то поле `union`, которое было записано последним? ⭐⭐
- Что может означать чтение другого поля `union`, не того, которое было записано последним?
  Всегда ли полученное значение имеет смысл? ⭐⭐⭐
- Почему пример `union { int salary; int gender; }` концептуально опасен, хотя оба поля имеют одинаковый тип? ⭐⭐

## Tagged union и безопасное хранение вариантов данных

- Что такое tagged union и какую проблему обычного `union` он решает? Как его описать? ⭐
- Напишите функцию, иллюстрирующую применение tagged union? ⭐⭐
- Как правильно работать с tagged union? ⭐⭐
- Как `switch` по тегу помогает сделать работу с объединением более безопасной и понятной? ⭐
- Почему tagged union широко используется в интерпретаторах, компиляторах,
  виртуальных машинах и библиотеках обработки данных? ⭐⭐
- Как размер tagged union зависит от размера тега,
  размера самого большого варианта и выравнивания структуры? ⭐⭐⭐
- Какие преимущества и недостатки имеет tagged union по сравнению с хранением
  всех возможных полей одновременно в обычной структуре? ⭐⭐
- Напишите tagged union `Variable`, чтобы он мог хранить строку,
  вещественное число или логическое значение? Какие вопросы управления памятью при этом возникнут? ⭐⭐⭐
- Как связаны между собой указатели, массивы, битовые поля и `union` с общей идеей интерпретации памяти в C? ⭐⭐
- Как вы понимаете фразу: "память хранит только байты, а типы языка C задают смысл этих байтов"? ⭐⭐
- Какие механизмы C позволяют программисту явно управлять представлением данных в памяти? \
  Сравните указатели, `unsigned char*`, битовые операции, битовые поля и `union`. ⭐⭐⭐
- В каких случаях программисту важно понимать реальное байтовое представление объектов в памяти? ⭐⭐
- Почему код, работающий с низкоуровневым представлением данных,
  часто оказывается зависимым от архитектуры, компилятора и ABI? ⭐⭐⭐
- Как выбрать между битовыми полями и ручными масками при проектировании структуры для хранения флагов? ⭐⭐
- Как выбрать между `union`, `memcpy` и побайтовым чтением через `unsigned char*`
  при необходимости посмотреть внутреннее представление объекта? ⭐⭐⭐
- Почему низкоуровневый код на C требует строгого различения между «работает на моей машине»
  и «гарантировано стандартом языка»? ⭐⭐⭐

## Представление дробных чисел: fixed-point, rational и floating point

- Какие ограничения возникают, если пытаться хранить дробные числа теми же способами, что и целые? ⭐⭐
- Почему задача представления действительных чисел в ограниченном числе бит принципиально
- связана с компромиссом между диапазоном и точностью? ⭐⭐
- Чем отличается математическое понятие действительного числа от машинного представления числа с плавающей запятой? ⭐⭐
- Почему дробная часть в Q32-формате хранится как `uint32_t`, а не как знаковое число? ⭐⭐
- Почему fixed-point арифметика часто используется в embedded-системах? ⭐⭐
- Какие преимущества fixed-point формата по сравнению с floating point? ⭐⭐
- Какие ограничения fixed-point формата делают его неудобным как универсальное представление вещественных чисел? ⭐⭐
- Как в fixed-point сложении обнаруживается переполнение дробной части? ⭐⭐
- Почему условие `result.fraction < a.fraction` позволяет обнаружить перенос при сложении `uint32_t`? ⭐⭐
- Почему число `-0.25` в представленном fixed-point формате можно хранить как `integer = -1`, `fraction = 0.75`? ⭐⭐⭐
- Почему рациональные числа позволяют точно представить многие значения, которые floating point хранит приближённо? ⭐⭐
- Почему при сложении рациональных чисел числитель и знаменатель могут быстро расти? ⭐⭐
- Зачем нужно сокращать рациональные дроби после операций? ⭐⭐
- Почему рациональное представление редко используется напрямую на уровне процессорной арифметики? ⭐⭐
- В каких задачах рациональное представление может быть предпочтительнее floating point? ⭐⭐
- Что такое нормализованная форма числа и зачем она нужна? ⭐⭐
- Как число `6.5` представляется в двоичной научной нотации? ⭐⭐
- Почему floating point позволяет хранить числа очень разных масштабов в одном формате? ⭐⭐
- Какую цену мы платим за большой диапазон чисел в floating point? ⭐⭐

## Формат IEEE 754 и битовое представление чисел

- Что такое bias в IEEE 754 и зачем он нужен? ⭐⭐
- Почему экспонента в IEEE 754 хранится со смещением, а не как обычное знаковое число? ⭐⭐
- Что означает формула `(-1)^sign × 1.mantissa × 2^(exponent - bias)`? ⭐⭐
- Почему ведущая единица мантиссы в нормализованных числах не хранится явно? ⭐⭐
- Как скрытая единица увеличивает эффективную точность представления числа? ⭐⭐
- Почему битовое представление `1.0f` равно `0x3F800000`? ⭐⭐⭐
- Как по битам `float` извлечь знак, экспоненту и мантиссу? ⭐⭐
- Почему для просмотра битового представления `float` в C лучше использовать `memcpy`, а не приведение указателей? ⭐⭐⭐
- Чем нормализованные числа отличаются от денормализованных? ⭐⭐
- Почему у нормализованных чисел мантисса имеет вид `1.xxx`, а у денормализованных — `0.xxx`? ⭐⭐
- Что происходит, когда поле экспоненты в IEEE 754 равно нулю? ⭐⭐
- Зачем нужны денормализованные числа? ⭐⭐
- Что означает gradual underflow и какую проблему он решает? ⭐⭐⭐
- Какие значения кодируются при экспоненте, состоящей из всех единиц? ⭐⭐
- Почему стандарт IEEE 754 вводит специальные значения вместо немедленной остановки программы при ошибке вычислений? ⭐⭐
- Почему в IEEE 754 существуют два нуля: `+0` и `-0`? ⭐⭐
- Почему выражения `1.0 / +0.0` и `1.0 / -0.0` дают разные результаты? ⭐⭐
- Как знак нуля связан с понятием предельного перехода в численных вычислениях? ⭐⭐⭐

## Floating point в языке C: типы, ввод-вывод и преобразования

- Почему `float` обычно даёт около 6–7 значащих цифр, а `double` — около 15–16? ⭐⭐
- Почему `long double` нельзя считать строго одинаковым типом на всех платформах? ⭐⭐
- Какие риски возникают при переносе C-кода с floating point между разными архитектурами? ⭐⭐⭐
- Почему в `printf` значение типа `float` автоматически передаётся как `double`? ⭐⭐
- Почему для `scanf` нужно использовать `%f` для `float`, но `%lf` для `double`? ⭐⭐
- Что может произойти, если перепутать спецификаторы формата при вводе чисел с плавающей запятой? ⭐⭐
- Как точность вывода, например `%.6f` или `%.17g`, влияет на то, что видит пользователь? ⭐⭐
- Почему вывод `3.14` с большим числом знаков может показать неожиданные цифры после запятой? ⭐⭐
- Чем отличаются выражения `5 / 2`, `5 / 2.0` и `(double)5 / 2`? ⭐⭐
- Почему автоматические преобразования типов могут быть источником ошибок в численных программах? ⭐⭐
- Как порядок преобразований `int → float → double → long double` влияет на вычисления? ⭐⭐
- Почему многие десятичные дроби, например `0.1`, нельзя точно представить в двоичной системе? ⭐⭐
- Почему точность чисел с плавающей запятой распределена неравномерно? ⭐⭐
- Почему при сложении большого и маленького числа маленькое может «потеряться»? ⭐⭐
- Что показывает пример `1e16 + 1.0 == 1e16`? ⭐⭐
- Почему операции с floating point не являются ассоциативными? ⭐⭐
- Как пример `(a + b) + c` и `a + (b + c)` демонстрирует зависимость результата от порядка вычислений? ⭐⭐

## Точность, сравнение, epsilon и численная устойчивость

- Почему численные алгоритмы должны учитывать порядок операций? ⭐⭐
- Какие последствия может иметь накопление ошибок округления в длинных вычислениях? ⭐⭐⭐
- Почему machine epsilon определяется относительно числа `1.0`? ⭐⭐
- Почему для `double` machine epsilon примерно равно `2.22 × 10^-16`? ⭐⭐
- Чем machine epsilon отличается от минимального положительного представимого числа? ⭐⭐⭐
- Почему расстояние между соседними floating point числами зависит от масштаба? ⭐⭐
- Как программа с циклом деления `epsilon` на два позволяет найти machine epsilon? ⭐⭐
- Почему epsilon около `1.0` нельзя бездумно использовать для сравнения любых больших или маленьких чисел? ⭐⭐⭐
- Почему выбор `eps` зависит от конкретной задачи? ⭐⭐
- Почему для очень больших чисел одной абсолютной погрешности может быть недостаточно? ⭐⭐
- Что такое относительная погрешность и почему она полезна при сравнении floating point чисел? ⭐⭐
- Когда прямое сравнение floating point чисел всё же может быть допустимо? ⭐⭐⭐
- Почему порядок суммирования массива чисел с плавающей запятой влияет на результат? ⭐⭐
- Почему полезно сначала складывать числа с меньшим модулем, а затем с большим? ⭐⭐
- Как сортировка по модулю может повысить точность суммирования? ⭐⭐
- Что такое алгоритм Кэхэна и какую проблему он решает? ⭐⭐⭐
- Почему частичные суммы могут терять точность при добавлении чисел сильно разного масштаба? ⭐⭐
- Как можно уменьшить накопление ошибок округления при суммировании большого набора данных? ⭐⭐

## Особые значения, `math.h` и математические вычисления

- Почему NaN не равен даже самому себе? ⭐⭐
- Чем `NaN`, `+∞` и `-∞` полезны для обработки ошибок в численных программах? ⭐⭐
- Что произойдёт при вычислении функци `sqrt(-1.0)`? ⭐⭐
- Какие проблемы могут возникнуть, если программа не проверяет появление NaN в промежуточных результатах? ⭐⭐
- Почему сложные математические функции часто реализуются библиотечно, а не одной машинной инструкцией? ⭐⭐
- Какие особенности точности и производительности нужно учитывать при использовании `pow`, `sqrt`, `sin` и `cos`? ⭐⭐
- Какие ошибки могут возникнуть при вычислении выражения `sqrt(dx * dx + dy * dy)`
  для очень больших значений `dx` и `dy`? ⭐⭐⭐
- Почему десятичная дробь `0.1` хранится в binary floating point приближённо? ⭐⭐
- Чем fixed-point или decimal-типы лучше подходят для финансовых вычислений? ⭐⭐
- Почему выражение «floating point — для научных вычислений, деньги — через точные представления»
  является хорошим инженерным правилом? ⭐⭐
- Как выбрать между `float` и `double` в обычной прикладной программе? ⭐⭐
- Почему `double` может быть предпочтительнее `float` в научных вычислениях? ⭐⭐
- Как тестировать программу, активно использующую floating point, на численную устойчивость? ⭐⭐⭐
- Какие типичные ошибки новичков при работе с floating point в C вы можете назвать и объяснить? ⭐⭐

## Аппаратная поддержка чисел с плавающей запятой, embedded-системы и оптимизации

- Чем математический сопроцессор отличается от встроенного FPU? ⭐⭐
- Как SIMD-инструкции вроде SSE, AVX или NEON ускоряют вычисления с floating point? ⭐⭐
- Почему поддержка floating point в RISC-V является опциональной? ⭐⭐
- Почему на микроконтроллерах операции с `double` могут быть значительно медленнее, чем с `float`? ⭐⭐
- Почему в embedded-разработке выбор между `float`, `double` и fixed-point особенно важен? ⭐⭐
- Как выбрать между `float` и `double` в embedded-системе? ⭐⭐
- Какие вопросы нужно задать о платформе перед активным использованием floating point на микроконтроллере? ⭐⭐
- Почему `float` может быть предпочтительнее `double` в real-time и embedded-задачах? ⭐⭐
- Почему компилятор не всегда может свободно менять порядок операций с floating point? ⭐⭐
- Как наличие NaN, бесконечностей и signed zero ограничивает оптимизации компилятора? ⭐⭐⭐
- Почему выражения `(a + b) + c` и `a + (b + c)` не всегда эквивалентны для floating point? ⭐⭐
- Какие оптимизации может разрешить `-ffast-math`? ⭐⭐
- Почему `-ffast-math` может изменить численный результат программы? ⭐⭐
- В каких случаях использование `-ffast-math` может быть оправдано? ⭐⭐
- В каких случаях использование `-ffast-math` может быть опасным? ⭐⭐
- Почему ускорение от `fast-math` не гарантировано? ⭐⭐

## Файл как внешнее хранилище данных, потоки и стандартный ввод-вывод в C

- Почему файл можно рассматривать как способ преодоления ограничений оперативной памяти? ⭐⭐
- Что такое файл? ⭐⭐
- Почему программа не выполняет вычисления «внутри файла», а переносит данные между файлом и памятью? ⭐⭐
- Чем отличается время жизни данных в оперативной памяти и во внешней памяти? ⭐⭐
- Почему файл не содержит информации о типах данных, которые в нём хранятся? ⭐⭐
- Как один и тот же набор байтов может быть интерпретирован по-разному разными программами? ⭐⭐
- Почему тип данных является способом интерпретации байтов, а не свойством самих байтов? ⭐⭐
- В чём сходство между оперативной памятью и файлом с точки зрения представления данных? ⭐⭐
- В чём принципиальное различие между доступом к данным в RAM и в файле? ⭐⭐
- Почему файл нельзя считать полным аналогом массива в памяти? ⭐⭐
- Почему доступ к файлам обычно существенно медленнее доступа к оперативной памяти? ⭐⭐
- Как файлы позволяют разным процессам обмениваться данными через устойчивое хранилище? ⭐⭐
- Почему в C ввод-вывод организован через потоки, а не только через файлы? ⭐⭐
- В чём состоит идея унификации ввода-вывода через `FILE *`? ⭐⭐
- Какие данные концептуально хранит объект `FILE`? ⭐⭐
- Почему программист работает с функциями, принимающими `FILE *`, а не с внутренними полями структуры `FILE`? ⭐⭐
- Зачем разделять обычный вывод и сообщения об ошибках на `stdout` и `stderr`? ⭐⭐
- Почему `stderr` часто не буферизуется, а `stdout` обычно буферизуется? ⭐⭐
- Что означает перенаправление потоков в команде вида `./program < in.txt > out.txt 2> err.txt`? ⭐⭐
- Почему использование `fprintf(stderr, ...)` предпочтительнее для сообщений об ошибках, чем обычный `printf`? ⭐⭐
- В чём ограничения абстракции потоков стандартной библиотеки C? ⭐⭐
- Почему `stdio.h` не является полным интерфейсом файловой системы? ⭐⭐
- Когда стандартного файлового ввода-вывода C может быть недостаточно и нужны POSIX API или WinAPI? ⭐⭐⭐
- Что может произойти при открытии существующего файла в режиме `"w"`? ⭐⭐
- Чем отличаются текстовые и бинарные режимы открытия файла? ⭐⭐
- Почему ввод-вывод считается потенциально ненадёжной операцией? ⭐⭐
- Как возвращаемое значение `fprintf` может использоваться для обнаружения ошибки записи? ⭐⭐
- Как возвращаемое значение `fscanf` помогает обнаружить ошибку чтения или несоответствие формата? ⭐⭐
- Для чего используются функции `ferror` и `feof`? ⭐⭐
- Почему недостаточно просто проверить достижение конца файла, чтобы убедиться в отсутствии ошибок чтения? ⭐⭐
- Почему в реальной программе нужно проверять ошибки даже после примеров, где проверки опущены ради краткости? ⭐⭐

## Символьный, строковый и форматированный ввод-вывод

- Что делает функция `fgetc` и почему её результат имеет тип `int`? ⭐⭐
- Что делает функция `fgets` и почему она принимает размер буфера? ⭐⭐
- Почему данные, записанные через `fputs` и `fputc`, можно затем прочитать через `fgetc` и `fgets`? ⭐⭐
- Что означает утверждение, что файл не хранит информацию о функциях, которыми он был записан? ⭐⭐
- Как форматированный ввод-вывод преобразует данные между машинным представлением и текстом? ⭐⭐
- Почему при записи числа через `fprintf` в файле хранится текст, а не бинарное представление числа? ⭐⭐
- Какие ограничения возникают при использовании `fscanf` для чтения данных заранее заданного формата? ⭐⭐
- Почему две программы, обменивающиеся данными через текстовый файл, должны быть согласованы относительно формата? ⭐⭐
- Почему при записи структуры через `fprintf` в файл попадает не сама структура, а текстовое представление её полей? ⭐⭐
- Чем опасно несоответствие форматной строки фактическому содержимому файла? ⭐⭐⭐
- Почему последняя строка файла не всегда обязана заканчиваться символом `'\n'`? ⭐⭐
- Какие преобразования могут происходить при записи и чтении текстового файла? ⭐⭐
- Почему текстовый ввод-вывод не гарантирует побайтовую идентичность данных? ⭐⭐
- Почему позиция в текстовом файле может не соответствовать количеству символов, которые видит программа? ⭐⭐
- Как формат записи числа с плавающей точкой влияет на точность его восстановления? ⭐⭐
- Почему запись `double` через `%f` может привести к потере точности? ⭐⭐
- Чем отличается восстановление значения числа от восстановления его точного битового представления? ⭐⭐⭐
- При каких условиях стандарт C гарантирует корректное восстановление данных из текстового потока? ⭐⭐⭐
- Почему наличие пробельных символов перед `'\n'` может быть проблемой для восстановления данных? ⭐⭐⭐
- Почему текстовый формат удобен для человека, но не всегда подходит для точного хранения данных? ⭐⭐
- В каких задачах текстовый ввод-вывод предпочтительнее бинарного? ⭐⭐
- В каких задачах текстовый ввод-вывод может быть принципиально неподходящим? ⭐⭐
- Почему текстовое представление данных зависит не только от значений, но и от выбранного формата записи? ⭐⭐

## Бинарный ввод-вывод и представление данных, endianness

- Почему параметры `fwrite` и `fread` можно считать симметричными? ⭐⭐
- Почему при бинарной записи числа в файл сохраняется его машинное представление? ⭐⭐
- Как с помощью `memcmp` можно проверить побайтовую идентичность записанного и прочитанного значения? ⭐⭐
- Почему бинарный ввод-вывод обычно быстрее форматированного текстового ввода-вывода? ⭐⭐
- Почему бинарный ввод-вывод хорошо подходит для хранения структур? ⭐⭐
- Какие проблемы могут возникнуть при прямой записи структуры в бинарный файл? ⭐⭐
- Как порядок байтов влияет на переносимость бинарных файлов? ⭐⭐
- Почему размеры типов данных важны при проектировании бинарного формата? ⭐⭐
- Что такое выравнивание и padding, и почему они мешают переносимой записи структур? ⭐⭐⭐
- Почему бинарный файл часто привязан к конкретному представлению данных в памяти? ⭐⭐
- Почему мультимедиа, архивы и базы данных обычно используют бинарные форматы? ⭐⭐
- Почему бинарный формат требует строгого описания структуры файла? ⭐⭐
- Что такое little-endian и big-endian? ⭐⭐
- Почему endianness является свойством порядка хранения байтов, а не самого числа? ⭐⭐
- Почему один и тот же набор байтов может давать разные числовые значения на системах с разным порядком байтов? ⭐⭐
- Зачем явно преобразовывать числа в заданный порядок байтов перед записью в файл? ⭐⭐
- Как функция `U32ToLE` (из лекции) раскладывает 32-битное число на байты? ⭐⭐
- Как функция `LEToU32` (из лекции) восстанавливает число из массива байтов? ⭐⭐
- Чем отличаются функции преобразования в little-endian и big-endian? ⭐⭐
- Почему при проектировании файлового формата лучше явно зафиксировать порядок байтов? ⭐⭐
- Почему использование типов фиксированного размера, например `int32_t`, повышает переносимость бинарного формата? ⭐⭐
- Почему нельзя безоговорочно записывать поле типа `int` в файл, если формат должен быть переносимым? ⭐⭐
- Почему при переносимой записи структуры лучше преобразовывать каждое поле отдельно,
  а не писать структуру целиком как блок памяти? ⭐⭐⭐
- Какие проблемы могут возникнуть, если файл с бинарными данными прочитать без учёта его endianness? ⭐⭐
- Почему сетевые протоколы и файловые форматы обычно задают порядок байтов явно? ⭐⭐
- Как можно сделать бинарный формат независимым от архитектуры процессора? ⭐⭐⭐
- В чём разница между «точным сохранением байтов памяти» и «переносимым бинарным форматом»? ⭐⭐⭐

## Позиция в файле и произвольный доступ, буферизация, синхронизация и EOF

- Почему поток можно сравнить с указателем, движущимся по массиву байтов? ⭐⭐
- Чем `rewind` отличается от `fseek(f, 0, SEEK_SET)`? ⭐⭐
- Почему управление позицией особенно полезно в бинарных файлах? ⭐⭐
- Почему произвольный доступ к данным сложнее организовать в текстовом файле? ⭐⭐
- Как преобразования конца строки могут мешать точному позиционированию в текстовом потоке? ⭐⭐
- Почему стандарт C ограничивает использование `fseek` в текстовых потоках? ⭐⭐
- Как можно изменить один элемент массива, записанного в бинарный файл, не переписывая файл целиком? ⭐⭐
- Почему пример с перезаписью `uint32_t` демонстрирует преимущество бинарных файлов? ⭐⭐
- Какие проверки ошибок следует добавить в программу, использующую `fseek`, `fwrite` и `fread`? ⭐⭐
- В каких реальных системах используется произвольный доступ к данным по смещению? ⭐⭐
- Почему файловое смещение нельзя отождествлять с указателем в памяти? ⭐⭐
- В каких ситуациях может потребоваться явный вызов `fflush`? ⭐⭐
- Почему ошибка записи может проявиться только при `fclose`? ⭐⭐
- Чем опасно игнорирование результата `fclose` при записи файла? ⭐⭐
- Почему два потока, открытые для одного файла, имеют независимые буферы? ⭐⭐
- Почему поток чтения может не увидеть данные, записанные другим потоком, до вызова `fflush`? ⭐⭐
- Почему результат `fgetc` нужно сохранять в переменную типа `int`, а не `char`? ⭐⭐
- Почему цикл `while (!feof(f))` считается неправильным способом чтения файла? ⭐⭐
- Когда устанавливается флаг конца файла? ⭐⭐
- Как правильно организовать цикл чтения файла с проверкой результата операции? ⭐⭐
- Как отличить нормальное достижение конца файла от ошибки чтения? ⭐⭐
- Почему `feof` и `ferror` следует использовать после попытки чтения, а не вместо проверки результата чтения? ⭐⭐⭐
- Как буферизация, EOF и ошибки ввода-вывода связаны с необходимостью аккуратной проверки всех операций? ⭐⭐⭐

## Организация многофайловой программы и интерфейсы модулей

- Какие признаки указывают на то, что программу пора разделять на несколько модулей? ⭐⭐
- Почему большой монолитный файл усложняет сопровождение и коллективную разработку программы? ⭐⭐
- В чём опасность ситуации, когда функции ввода-вывода, обработки данных и основная логика находятся в одном файле? ⭐⭐
- Объясните, почему разбиение программы на модули повышает надёжность и читаемость кода. ⭐⭐
- Какие проблемы возникают при исправлении ошибки в коде, который был многократно скопирован в разные файлы? ⭐⭐
- Чем модульное переиспользование отличается от copy-paste подхода? ⭐⭐
- Почему модуль должен иметь чётко определённую ответственность? ⭐⭐
- Как модульная организация помогает расширять программу без полного переписывания существующего кода? ⭐⭐
- Почему интерфейс модуля принято отделять от его реализации? ⭐⭐
- Какие сущности обычно размещают в заголовочном файле?
  Объясните выбор. ⭐⭐
- Почему в заголовочный файл не следует включать лишние зависимости? ⭐⭐
- Зачем файл реализации обычно подключает собственный заголовочный файл? ⭐⭐
- Что может произойти, если объявление функции в `.h` не совпадает с её определением в `.c`? ⭐⭐
- Какими свойствами должен обладать хороший модуль в программе на C? ⭐⭐
- Почему интерфейс модуля должен быть минимальным, но достаточным? ⭐⭐⭐
- Как определить, какие функции следует вынести в `.h`, а какие оставить внутренними в `.c` файле? ⭐⭐
- Почему изменение реализации модуля не должно требовать изменения пользовательского кода? ⭐⭐
- Как связаны понятия «модульность», «инкапсуляция» и «сопровождаемость»? ⭐⭐⭐
- Приведите пример модуля для программы на C и объясните, что должно находиться в его `.h` и `.c` файлах. ⭐⭐
- Какие ошибки проектирования могут привести к тому,
  что формально многофайловая программа всё равно останется плохо структурированной? ⭐⭐⭐
- Почему программу на C полезно рассматривать не как набор файлов, а как систему модулей с чёткими интерфейсами? ⭐⭐⭐

## Сборка, компиляция, компоновка и заголовочные файлы

- Почему одного физического разделения программы на несколько `.c` файлов недостаточно для их совместной работы? ⭐⭐
- Почему компилятор не знает о типах и функциях, определённых в другом `.c` файле,
  если они не объявлены через заголовочный файл? ⭐⭐
- Чем отличается ошибка компиляции от ошибки компоновки? Приведите пример каждой. ⭐⭐
- Почему заголовочные файлы обычно не компилируются напрямую? ⭐⭐
- Почему раздельная компиляция особенно полезна в больших проектах? ⭐⭐
- Что произойдёт, если функция объявлена в заголовочном файле,
  но её реализация отсутствует во всех `.c` файлах проекта? ⭐⭐
- Почему нельзя вручную дублировать объявления структур и функций в каждом `.c` файле, где они нужны? ⭐⭐
- Что означает утверждение, что `#include` выполняет текстовую подстановку? ⭐⭐
- Почему обычные функции не следует определять в `.h` файле, если он подключается в несколько `.c` файлов? ⭐⭐
- Как возникает ошибка `multiple definition` при неправильном использовании заголовочных файлов? ⭐⭐
- Чем объявление функции отличается от её определения? ⭐⭐
- Почему вызов очень короткой функции может быть относительно дорогим? ⭐⭐
- Что означает встраивание функции компилятором? ⭐⭐
- Почему ключевое слово `inline` само по себе не гарантирует фактического встраивания функции? ⭐⭐
- Почему компилятору проще встроить функцию, если её тело видно в месте вызова? ⭐⭐
- В каких случаях inline-функция в заголовочном файле может быть оправданной? ⭐⭐
- Какие риски и ограничения есть у размещения реализаций функций в заголовочных файлах? ⭐⭐⭐

## Видимость, `static` и инкапсуляция данных

- Почему глобальные переменные, доступные из разных файлов, могут ухудшать структуру программы? ⭐⭐
- Почему рекомендуется скрывать всё, что не является частью интерфейса модуля? ⭐⭐
- В чём отличие публичной функции модуля от вспомогательной `static`-функции? ⭐⭐
- Как использование `static` помогает не только структурировать код, но и потенциально улучшать оптимизацию? ⭐⭐⭐
- Почему открытое определение структуры в заголовочном файле может нарушать инкапсуляцию? ⭐⭐
- Что такое неполный тип или opaque type в языке C? ⭐⭐
- Почему при использовании opaque type внешний код может работать с указателем на структуру,
  но не может обращаться к её полям? ⭐⭐⭐
- Зачем определение структуры переносят из `.h` файла в `.c` файл? ⭐⭐
- Почему при сокрытии структуры обычно нужны функции создания и уничтожения объекта? ⭐⭐
- Как opaque types позволяют изменять внутреннее устройство структуры без изменения внешнего интерфейса модуля? ⭐⭐⭐
- Почему opaque types помогают защищать инварианты структуры данных? ⭐⭐⭐
- Какие недостатки или усложнения может вносить использование opaque types? ⭐⭐⭐
- Почему сокрытие внутренних функций модуля уменьшает риск конфликтов имён в большой программе? ⭐⭐
- Чем файловая область видимости в C отличается от локальной области видимости внутри функции? ⭐⭐
- Почему использование `extern` следует ограничивать и применять только для действительно публичных объектов модуля? ⭐⭐
- Какие проблемы могут возникнуть, если пользователь модуля напрямую изменяет поля структуры,
  минуя функции интерфейса? ⭐⭐
- Почему opaque type часто используется совместно с динамическим выделением памяти? ⭐⭐
- Как можно организовать интерфейс модуля так, чтобы пользователь не мог создать некорректный объект? ⭐⭐⭐
- В чём разница между сокрытием вспомогательной функции через `static` и сокрытием структуры через opaque type? ⭐⭐⭐

## Указатели на функции: объявление, типы и типичные ошибки

- Чем указатель на функцию отличается от указателя на данные? ⭐⭐
- Почему запись `int cmp(int, int);` объявляет функцию, а не указатель на функцию? ⭐⭐
- Объявите указатель `Comparator`, способный хранить указатель функции `cmp`;
- Почему тип указателя на функцию должен точно соответствовать сигнатуре функции? ⭐⭐
- Что произойдёт, если попытаться присвоить указателю на функцию адрес функции с другим типом возвращаемого значения? ⭐⭐
- Почему указатели на функции позволяют рассматривать поведение программы как данные? ⭐⭐
- В каких ситуациях передача функции как аргумента делает код более гибким? ⭐⭐
- Как объявить переменную типа `Comparator` и присвоить ей функцию сравнения? ⭐⭐
- В каких случаях `typedef` может скрыть важные детали типа и затруднить понимание кода? ⭐⭐⭐
- Как отличить псевдоним типа указателя на функцию от псевдонима обычной функции? ⭐⭐
- Почему компилятор C строго проверяет типы указателей на функции? ⭐⭐
- Чем опасен вызов функции через указатель неправильного типа? ⭐⭐⭐
- Что произойдёт при попытке вызвать указатель на функцию, равный `NULL`? ⭐⭐
- Как отличить ошибку объявления функции от ошибки объявления указателя на функцию? ⭐⭐
- Почему нельзя считать указатель на функцию «обычным адресом», совместимым с любым другим указателем? ⭐⭐⭐

## Компараторы, универсальная сортировка и `qsort`

- Почему условие сравнения элементов можно считать отдельной ответственностью алгоритма сортировки? ⭐⭐
- Как компаратор позволяет отделить алгоритм сортировки от правила упорядочивания? ⭐⭐
- Почему компаратор обычно возвращает отрицательное число, ноль или положительное число? ⭐⭐
- Почему компаратор для сортировки по убыванию можно реализовать как отрицание компаратора по возрастанию? ⭐⭐
- Какие ошибки могут возникнуть, если компаратор нарушает согласованность порядка? ⭐⭐⭐
- Почему сортировка с компаратором является примером параметризации поведения? ⭐⭐
- Как изменить универсальную пузырьковую сортировку, чтобы она могла сортировать по последней цифре числа? ⭐⭐
- Как реализовать компаратор, который сначала ставит чётные числа, а затем нечётные? ⭐⭐
- Почему один и тот же алгоритм сортировки может использовать разные критерии упорядочивания без изменения своего кода? ⭐⭐
- Почему компаратор для `qsort` принимает параметры типа `const void*`? ⭐⭐
- Зачем внутри компаратора для `qsort` выполнять явное приведение типа указателей? ⭐⭐
- Почему `qsort` может сортировать массивы разных типов? ⭐⭐
- Какие ошибки возможны при неправильном приведении `const void*` внутри компаратора `qsort`? ⭐⭐⭐
- Почему нельзя просто вычесть два числа в компараторе как `return x - y;` во всех случаях? ⭐⭐⭐
- Как написать компаратор для `qsort`, сортирующий структуры по одному из полей? ⭐⭐
- Почему `qsort` является примером универсального алгоритма? ⭐⭐
- Как указатели на функции связаны с понятием обратного вызова? ⭐⭐
- Что такое callback-функция и чем она полезна? ⭐⭐
- Почему `qsort` можно рассматривать как пример использования callback-функции? ⭐⭐
- Как написать компаратор для сортировки чисел по абсолютному значению? ⭐⭐
- Как написать компаратор для сортировки чисел по последней цифре? ⭐⭐

## Указатели на функции в структурах и абстракция потока данных

- Зачем хранить указатели на функции внутри структур? ⭐⭐
- Как структура может хранить одновременно данные и поведение? ⭐⭐
- Почему хранение функций в структуре позволяет реализовать разные варианты поведения через единый интерфейс? ⭐⭐
- Что означает фраза «поведение определяется содержимым структуры во время выполнения»? ⭐⭐
- Чем отличается вызов обычной функции от вызова функции через поле структуры? ⭐⭐
- Какие риски появляются, если поле структуры с указателем на функцию не инициализировано? ⭐⭐
- Почему указатели на функции в структурах часто используются при проектировании библиотек на C? ⭐⭐
- Как указатели на функции помогают создать единый интерфейс чтения и записи? ⭐⭐
- Какую роль играет поле `context` в структуре `Stream` из лекции? ⭐⭐
- Почему `context` имеет тип `void*`, а не `char*`? ⭐⭐
- Как функция `StreamRead` выбирает, какую реализацию чтения вызвать? ⭐⭐
- Почему в `StreamRead` сначала проверяется пользовательская функция чтения, а затем `FILE*`? ⭐⭐
- Как реализовать поток, который читает данные не из файла, а из сетевого буфера или устройства? ⭐⭐⭐
- Почему `Stream` можно рассматривать как пример ручной реализации полиморфизма в C? ⭐⭐⭐

## Потоки в памяти, контекст и безопасность работы с буферами

- Почему перед чтением из памяти нужно вычислять количество доступных байт? ⭐⭐
- Как функция `MemoryRead` из лекции защищает программу от выхода за границы буфера? ⭐⭐
- Как `MemoryRead` соответствует общему типу `ReadFunction`,
  несмотря на то что работает с конкретной структурой `MemoryStream`? ⭐⭐
- Почему внутри `MemoryRead` необходимо привести `void* context` к `MemoryStream*`? ⭐⭐
- Какие ошибки возможны, если в `context` передан указатель не на тот тип данных? ⭐⭐⭐
- Как можно изменить `MemoryWrite`, чтобы она сообщала об ошибке переполнения буфера, а не просто ограничивала размер записи? ⭐⭐⭐
- Как добавить в `Stream` поддержку только чтения без записи? ⭐⭐
- Как добавить новый тип потока, не изменяя код `StreamRead` и `StreamWrite`? ⭐⭐⭐
- Как проверить корректность инициализации структуры `Stream` перед первым чтением или записью? ⭐⭐
- Почему при работе с универсальным `context` возрастает ответственность программиста за корректность типов? ⭐⭐⭐
- Как можно организовать несколько разных реализаций чтения и записи через один и тот же интерфейс `Stream`? ⭐⭐
- Какие преимущества даёт отделение интерфейса потока от конкретного источника данных? ⭐⭐
- Какие проблемы могут возникнуть, если функция чтения или записи изменяет состояние контекста некорректно? ⭐⭐⭐
- Почему обновление позиции в `MemoryStream` является частью состояния потока, а не внешней логики пользователя? ⭐⭐
- В чём отличие выбора поведения на этапе компиляции от выбора поведения во время выполнения? ⭐⭐⭐

## Единый интерфейс, opaque type и таблица виртуальных функций

- Что такое opaque type и зачем он используется? ⭐⭐
- Почему в заголовочном файле можно объявить `typedef struct Shape Shape;`, не раскрывая содержимое структуры? ⭐⭐
- Как скрытие реализации помогает уменьшить связанность между модулями? ⭐⭐
- Почему фабричные функции вроде `CreateCircle` и `CreateRectangle` возвращают `Shape*`,
  а не `Circle*` или `Rectangle*`? ⭐⭐
- Как можно хранить разные типы фигур в одном массиве? ⭐⭐
- Почему массив `Shape* shapes[]` может содержать и окружности, и прямоугольники? ⭐⭐
- Что такое таблица виртуальных функций в реализации на C? ⭐⭐
- Почему каждая конкретная фигура имеет собственную таблицу виртуальных функций? ⭐⭐
- Как поле `vtbl` в структуре `Shape` помогает выбрать нужную реализацию операции? ⭐⭐
- Что происходит при вызове `ShapeDraw(s)`? ⭐⭐
- Почему `ShapeDraw` не должен знать, является ли объект окружностью или прямоугольником? ⭐⭐
- Как `ShapeGetArea` реализует динамический выбор поведения? ⭐⭐
- Какие ошибки могут возникнуть, если поле `vtbl` не было инициализировано? ⭐⭐⭐
- Какие изменения потребуются в таблице виртуальных функций, если нужно добавить операцию масштабирования фигуры? ⭐⭐⭐
- Как организовать массив объектов, чтобы в нём можно было хранить разные реализации одного интерфейса? ⭐⭐
- Почему передача функции как параметра уменьшает дублирование кода в функциях вроде `BubbleSort`? ⭐⭐

## Базовая структура, приведение типов, создание и уничтожение объектов

- Почему структура `Circle` начинается с поля `Shape base`? ⭐⭐
- Почему важно, чтобы базовая структура была первым полем конкретной структуры? ⭐⭐⭐
- Как указатель `Shape*` внутри функции `DrawCircle` преобразуется обратно к `Circle*`? ⭐⭐
- При каких условиях такое приведение указателя является корректным? ⭐⭐⭐
- Почему функция `DrawCircle` принимает `const Shape*`, а не `const Circle*`? ⭐⭐
- Что может произойти, если передать в функцию `DrawCircle` объект, который на самом деле не является окружностью? ⭐⭐⭐
- Почему функции `DrawCircle` и `GetCircleArea` объявлены как `static`? ⭐⭐
- Как `static` помогает скрыть детали реализации модуля? ⭐⭐
- Почему при создании фигуры особенно важно правильно записать указатель на таблицу виртуальных функций? ⭐⭐
- Почему `CreateCircle` возвращает `(Shape*)c`? ⭐⭐
- Какие проблемы возникнут, если фабричная функция вернёт неинициализированный объект? ⭐⭐⭐
- Когда простой реализации `DestroyShape` через `free` может быть недостаточно? ⭐⭐⭐
- Как можно расширить таблицу виртуальных функций, чтобы каждая фигура имела собственный деструктор? ⭐⭐⭐
- Как добавить новый тип фигуры, например треугольник, в существующую систему `Shape`? ⭐⭐
- Как указатели на функции помогают реализовать принцип разделения ответственности? ⭐⭐

## Организация адресного пространства и областей памяти

- Почему память программы нельзя рассматривать как простой непрерывный набор байтов без внутренней структуры? ⭐⭐
- Какие основные области памяти процесса обычно выделяют в современных операционных системах
  и каково назначение каждой из них? ⭐⭐
- Чем отличаются сегменты text, data и BSS с точки зрения размещаемых в них объектов? ⭐⭐
- Почему код программы и данные обычно размещаются в разных областях памяти? ⭐⭐
- Какую роль операционная система играет в организации адресного пространства процесса? ⭐⭐
- Почему две переменные, объявленные рядом в исходном коде, не обязательно будут расположены рядом в памяти? ⭐⭐
- Почему область кода и область констант часто защищаются от записи? ⭐⭐
- Как права доступа к различным областям памяти повышают надёжность и безопасность программы? ⭐⭐⭐
- Какие факторы, кроме исходного кода программы, влияют на фактическое расположение объектов в памяти? ⭐⭐⭐
- Что такое адресное пространство процесса и почему каждый процесс обычно получает собственное адресное пространство? ⭐⭐
- Как загрузчик и операционная система участвуют в размещении сегментов программы в памяти? ⭐⭐⭐
- Почему понимание структуры памяти особенно важно при программировании на языке C? ⭐⭐
- Почему переменные, относящиеся к разным сегментам памяти, могут иметь существенно различающиеся адреса? ⭐⭐
- Почему расположение объектов в памяти определяется не только компилятором, но и операционной системой? ⭐⭐⭐

## Стек, кадры функций и возврат управления

- Что такое кадр стека функции и какие данные он может содержать? ⭐⭐
- Почему при каждом вызове функции создаётся новый кадр стека? ⭐⭐
- Почему содержимое удалённого стекового кадра может физически оставаться в памяти? ⭐⭐
- Почему память, оставшаяся после завершения функции, не должна рассматриваться как корректные данные? ⭐⭐
- Как стек отражает текущую цепочку вложенных вызовов функций? ⭐⭐
- Почему адрес локальной переменной может совпадать с адресом локальной переменной
  из предыдущего вызова другой функции? ⭐⭐
- Почему адрес локальной переменной зависит не только от самой функции, но и от текущего состояния стека? ⭐⭐⭐
- Что такое адрес возврата и зачем он сохраняется при вызове функции? ⭐⭐
- Как инструкция `call` на уровне процессора связана с изменением стека? ⭐⭐⭐
- Как инструкция `ret` использует стек для возврата из функции? ⭐⭐⭐
- Для чего в некоторых архитектурах адреса возврата хранятся отдельно от обычных данных стека? ⭐⭐⭐
- Как отдельный стек возвратов может повысить безопасность программы? ⭐⭐⭐
- Почему переполнение стека может быть опасным не только для данных, но и для управления выполнением программы? ⭐⭐⭐
- Чем стек отличается от кучи с точки зрения скорости выделения и освобождения памяти? ⭐⭐
- Почему стек обычно быстрее кучи при работе с небольшими временными объектами? ⭐⭐
- Какие ограничения есть у размещения данных на стеке? ⭐⭐

## Соглашения о вызовах, регистры и ассемблерный уровень

- Что такое соглашение о вызовах и зачем оно нужно компилятору, функциям и библиотекам? ⭐⭐
- Почему параметры функции во многих ABI передаются через регистры, а не через стек? ⭐⭐
- Почему локальная переменная, логически принадлежащая функции, не всегда физически находится в оперативной памяти? ⭐⭐
- В каких случаях компилятор вынужден сохранить значение переменной из регистра в память? ⭐⭐⭐
- Почему взятие адреса переменной может повлиять на то, будет ли она размещена в регистре или в стеке? ⭐⭐⭐
- Что такое выравнивание стека и почему оно важно перед вызовом функции? ⭐⭐⭐
- Как вызов функции выглядит на уровне ассемблера:
  какие действия выполняются до перехода, внутри функции и при возврате? ⭐⭐⭐
- Как передача аргументов через регистры влияет на производительность вызова функции? ⭐⭐
- Почему сохранение адреса возврата является частью механизма передачи управления? ⭐⭐
- Как работа со стековым указателем связана с выделением и освобождением локальных данных функции? ⭐⭐
- Почему на уровне ассемблера даже простая функция может содержать инструкции, связанные с памятью и выравниванием? ⭐⭐⭐

## Время жизни объектов и ошибки доступа к памяти

- Чем время жизни объекта отличается от области видимости имени переменной? ⭐⭐
- Почему объект может физически оставаться в памяти после завершения своего времени жизни? ⭐⭐
- Почему наличие адреса объекта не означает, что объект всё ещё существует? ⭐⭐
- Что такое висячий указатель и почему он опасен? ⭐⭐
- Почему возврат адреса локальной переменной из функции является ошибкой? ⭐⭐
- Что такое неопределённое поведение и почему оно особенно опасно при ошибках работы с памятью? ⭐⭐⭐
- Как время жизни локальных, глобальных, статических и динамических объектов связано с областями памяти? ⭐⭐
- Почему программист должен различать физическое содержимое памяти
  и корректное существование объекта с точки зрения языка C? ⭐⭐⭐
- Почему доступ к объекту вне его lifetime считается ошибкой, даже если данные ещё видны в памяти? ⭐⭐
- Как ошибки времени жизни объектов связаны с использованием указателей? ⭐⭐
- Чем отличается ошибка доступа к уже уничтоженному объекту от обычной логической ошибки в программе? ⭐⭐⭐

## Куча, динамическая память и статическое состояние

- Чем время жизни объекта в куче отличается от времени жизни локальной переменной? ⭐⭐
- Почему динамическая память даёт программисту большую гибкость, но требует большей дисциплины? ⭐⭐
- Почему использование неинициализированной памяти, полученной через `malloc`, может приводить к ошибкам? ⭐⭐
- Что такое утечка памяти и почему она опасна для долгоживущих программ? ⭐⭐
- Что такое use-after-free и почему эта ошибка считается особенно серьёзной? ⭐⭐⭐
- Почему после вызова `free` указатель сам по себе не исчезает и может стать источником ошибки? ⭐⭐
- Почему частые вызовы `malloc` и `free` внутри циклов могут ухудшать производительность программы? ⭐⭐
- Как можно уменьшить количество динамических выделений памяти в программе? ⭐⭐
- В каких случаях использование кучи оправдано, несмотря на её накладные расходы? ⭐⭐
- Почему управление памятью в куче особенно важно в программах на C,
  но менее заметно в языках с автоматическим управлением памятью? ⭐⭐
- За счёт чего статическая переменная функции сохраняет значение между вызовами? ⭐⭐
- Как статическая переменная может использоваться для хранения состояния внутри функции? ⭐⭐
- Чем время жизни статической переменной отличается от её области видимости? ⭐⭐
- Какие преимущества и недостатки есть у использования статических переменных внутри функций? ⭐⭐
- Как различаются глобальные переменные, статические переменные и локальные переменные с точки зрения lifetime? ⭐⭐
- Почему чрезмерное использование глобального или статического состояния может усложнять программу? ⭐⭐

## Иерархия памяти, кеш и cache line

- Почему регистры быстрее кеша, а кеш быстрее оперативной памяти? ⭐⭐
- Почему самый быстрый уровень памяти обычно имеет наименьший объём? ⭐⭐
- Почему доступ к оперативной памяти может быть во много раз медленнее доступа к L1-кешу? ⭐⭐
- Как разница в скорости между процессором и оперативной памятью влияет на производительность программ? ⭐⭐
- Почему современная программа может тратить значительную часть времени не на вычисления, а на ожидание данных? ⭐⭐⭐
- Почему иерархия памяти в основном прозрачна для программиста на C,
  но всё равно влияет на производительность его кода? ⭐⭐
- Почему производительность программы зависит не только от количества операций, но и от характера доступа к памяти? ⭐⭐
- Как понимание иерархии памяти помогает писать более быстрые программы? ⭐⭐
- Почему при cache miss в кеш загружается не один байт, а целая cache line? ⭐⭐
- Что такое cache line и почему её размер важен для производительности? ⭐⭐
- Почему последовательный обход массива хорошо использует cache line? ⭐⭐
- Почему доступ к памяти с большим шагом может приводить к большому количеству cache miss? ⭐⭐
- Как размер элемента массива влияет на количество элементов, помещающихся в одну cache line? ⭐⭐
- Почему компактное представление данных может улучшить использование кеша? ⭐⭐
- Как cache miss может изменить реальное время выполнения программы без изменения её алгоритмической сложности? ⭐⭐⭐
- Почему при анализе производительности важно учитывать не только инструкции процессора, но и обращения к памяти? ⭐⭐⭐
- Почему кеш-память эффективна только при определённых закономерностях доступа к данным? ⭐⭐
- Как загрузка cache line объясняет ускорение последовательного доступа к массиву? ⭐⭐

## Локальность доступа и организация данных

- Почему последовательный обход массива обычно быстрее случайного доступа к элементам? ⭐⭐
- Почему связные списки часто хуже используют кеш, чем массивы? ⭐⭐
- Как порядок обхода двумерного массива в C связан с его размещением в памяти? ⭐⭐
- Почему обход матрицы по строкам обычно быстрее обхода по столбцам в языке C? ⭐⭐
- Почему две программы с одинаковым количеством операций могут иметь радикально разное время выполнения? ⭐⭐⭐
- Как аппаратная предвыборка данных помогает при последовательном доступе к памяти? ⭐⭐⭐
- Почему аппаратный prefetcher плохо помогает при псевдослучайном доступе? ⭐⭐⭐
- Почему доступ внутри одной cache line может показывать небольшую разницу между последовательным и псевдослучайным порядком? ⭐⭐⭐
- Почему при большом массиве псевдослучайный доступ становится значительно медленнее последовательного? ⭐⭐⭐
- Как можно изменить структуру данных, чтобы улучшить пространственную локальность? ⭐⭐
- Как можно изменить алгоритм обхода данных, чтобы уменьшить количество cache miss? ⭐⭐
- Почему локальность доступа иногда важнее микроскопических оптимизаций отдельных инструкций? ⭐⭐⭐
- Как понятия spatial locality и temporal locality помогают объяснить поведение кеш-памяти? ⭐⭐
- Почему массивы обычно обеспечивают лучшую пространственную локальность, чем структуры с указателями? ⭐⭐
- Как размер структуры данных влияет на эффективность использования кеша? ⭐⭐
- Почему разреженный доступ к памяти может быть дорогим даже при простых вычислениях? ⭐⭐
- Как временная локальность проявляется при повторном использовании одних и тех же переменных в цикле? ⭐⭐
- Почему порядок размещения и порядок обработки данных должны рассматриваться совместно? ⭐⭐⭐
- Как локальность доступа связывает архитектуру процессора с выбором структуры данных? ⭐⭐⭐

# Ассемблер

## Архитектура процессора Intel 8080: машинный код, ISA, модель выполнения, регистры, работа с памятью, флаги

- Объясните, какие уровни представления проходит программа от C-кода до байтов в памяти,
   и покажите, на каком уровне исчезают понятия переменных, выражений и типов. ⭐⭐
- Дан фрагмент C-кода `c = a + b;`, где `a`, `b`, `c` — байтовые переменные в памяти.
  Опишите возможную последовательность действий процессора Intel 8080 для выполнения этой операции. ⭐⭐⭐
- Сравните программную модель Intel 8080 и физическую реализацию процессора:
  какие элементы обязана “видеть” программа, а какие могут быть реализованы по-разному? ⭐⭐⭐
- Опишите полный цикл выполнения инструкции Intel 8080. ⭐⭐⭐
- Что произойдёт, если процессор начнёт выполнение программы не с начала инструкции,
  а со второго или третьего байта многобайтовой команды? ⭐⭐⭐
- Объясните, почему одну и ту же область памяти можно интерпретировать и как данные,
  и как машинный код. Какие риски и возможности из этого следуют? ⭐⭐⭐
- Перечислите регистры Intel 8080, которые могут использоваться как 8-битные регистры общего назначения,
  и укажите, какие из них образуют 16-битные регистровые пары. ⭐⭐
- Сколько байт занимает указатель в системе с 16-битной адресацией?
  Как это влияет на хранение указателей в памяти и на использование инструкций `LHLD` / `SHLD`? ⭐⭐
- Сравните роли регистровых пар `BC`, `DE` и `HL` в типичных программах Intel 8080. ⭐⭐
- Почему регистровая пара `HL` имеет особое значение при обращении к памяти через обозначение `M`,
  а `BC` и `DE` требуют других инструкций для похожих операций? ⭐⭐⭐
- Дан адрес `1234H`. В каком порядке его байты будут храниться в машинном коде инструкции `LXI H,1234H`? ⭐⭐
- Сравните инструкции `LDA 1000H`, `STA 1000H`, `MOV A,M`, `MOV M,A`, `LHLD 1000H`, `SHLD 1000H`
  по типу адресации, размеру данных и роли аккумулятора или пары `HL`. ⭐⭐⭐
- Почему в Intel 8080 большинство операций с памятью приходится строить по схеме
  “загрузить в регистр → обработать → записать обратно”? Приведите пример для операции над двумя байтами в памяти. ⭐⭐⭐
- Дан фрагмент:

  ```asm
  LXI H,1000H
  MOV A,M
  INX H
  ADD M
  STA 2000H
  ```

  Что делает эта программа? ⭐⭐⭐

- Для чего аккумулятор `A` используется как центральный регистр арифметико-логических операций?
  Какие ограничения это накладывает на трансляцию выражений из C? ⭐⭐
- Составьте последовательность команд Intel 8080 для вычисления `x = y + z`,
  если `x`, `y`, `z` — байтовые переменные по адресам `1000H`, `1001H`, `1002H`. ⭐⭐⭐
- Составьте последовательность команд для вычисления `x = y - z`, если `y` и `z` лежат в памяти,
  а результат нужно сохранить в `x`. Укажите, где будет находиться промежуточный результат. ⭐⭐⭐
- Чем отличаются инструкции `ADD B`, `ADD M`, `INR M` и `INX H` по объекту изменения,
  использованию `HL` и влиянию на смысл программы? ⭐⭐⭐
- После каких типов операций в Intel 8080 имеет смысл анализировать флаг `Z`, а после каких — флаг `CY`?
  Приведите примеры использования обоих флагов. ⭐⭐⭐
- Почему флаги состояния можно использовать как скрытый механизм передачи результата
  от арифметической или логической инструкции к условному переходу? ⭐⭐⭐
- Как проверить, что 16-битное значение в паре `DE` равно нулю? ⭐⭐⭐

- Дан фрагмент:

  ```asm
  MOV A,B
  ORA C
  JNZ LOOP
  ```

  При каких значениях `B` и `C` переход будет выполнен? При каких не будет? ⭐⭐

- Почему при написании длинного фрагмента на ассемблере важно учитывать, какие команды изменяют флаги,
  даже если результат этих команд явно не используется? ⭐⭐⭐

## Архитектура процессора Intel 8080: управление выполнением, циклы и ветвления

- Опишите, как условный оператор `if (x == 0)` (при 16-битном x) может быть представлен на Intel 8080. ⭐⭐⭐
- Чем машинная реализация `while` отличается от машинной реализации `do-while`? ⭐⭐⭐
- Дан фрагмент:

  ```asm
  JMP CHECK
  LOOP:
      ; тело цикла
  CHECK:
      MOV A,B
      ORA C
      JNZ LOOP
  ```

  Опишите поведение программы при `BC = 0000H` и при `BC = 0003H`. ⭐⭐⭐

- Как изменение регистра `PC` связано с реализацией переходов, вызовов подпрограмм и возвратов?
  Сравните `JMP`, `CALL` и `RET`. ⭐⭐⭐
- Почему команда `RET` не содержит адрес возврата в своём машинном коде?
  Где находится этот адрес и как он используется? ⭐⭐⭐
- Переведите операцию `x1 = x2`, если `x1` расположен по адресу `1000H`,
  а `x2` — по адресу `1001H`. ⭐⭐
- Переведите операцию `*ptr1 = *ptr2`, если `ptr1` хранится по адресу `1002H`,
  а `ptr2` — по адресу `1004H`. Укажите, где используется значение указателя, а где значение по указателю. ⭐⭐⭐

- Дан C-фрагмент:

  ```c
  uint8_t *p = array;
  *p = 42;
  p++;
  *p = 43;
  ```

  Постройте возможный ассемблерный вариант для массива, начинающегося с адреса `1000H`. ⭐⭐⭐

- Как операция `*(p++) = v` раскладывается на изменение памяти и изменение указателя? ⭐⭐⭐
- Сравните реализацию работы с массивом байтов и массивом 16-битных значений на Intel 8080.
  Как изменятся шаг указателя и операции чтения/записи? ⭐⭐⭐
- Сколько байт занимает инструкция `MOV A,B`, сколько — `MVI A,5`, сколько — `STA 1000H`? Объясните различие. ⭐
- Получите машинный код команды `MOV A,B`, используя формат `01 DDD SSS`, где код `A = 111`, код `B = 000`. ⭐
- Получите машинный код команды `MVI A,05H`, если известно, что opcode для `MVI A,data` равен `3EH`. Укажите все байты инструкции. ⭐⭐
- Получите машинный код команды `LXI H,1000H`, если opcode равен `21H`. Объясните полученный результат. ⭐⭐
- По последовательности байтов `21 0C 00 3E 2A 77 76` восстановите ассемблерную программу
  и укажите адрес начала каждой инструкции, если первый байт расположен по адресу `0000H`. ⭐⭐⭐
- Почему карта инструкций Intel 8080 имеет регулярную структуру для групп команд вроде `MOV`,
  но также содержит фиксированные opcode для команд `NOP`, `RET`, `HLT`? ⭐⭐⭐

## Архитектура x86-64: регистры общего назначения и специальные регистры

- Сколько регистров общего назначения доступно в x86-64 и почему их стало больше по сравнению с ранними x86? ⭐⭐
- Какие традиционные роли имеют регистры RAX, RBX, RCX и RDX? ⭐⭐
- Почему в современных программах традиционные роли регистров не являются жёсткими? ⭐⭐
- Чем отличаются RSP и RBP с точки зрения работы со стеком? ⭐⭐
- Почему RSP нельзя произвольно использовать как обычный регистр общего назначения? ⭐⭐
- Что означает утверждение, что RAX, EAX, AX, AL и AH — это разные представления одного регистра? ⭐⭐
- Что произойдёт со старшими 32 битами RAX при записи значения в EAX? ⭐⭐
- Почему запись в AX или AL может приводить к ошибкам, если программист забывает о старших битах регистра? ⭐⭐⭐
- Чем работа с частями регистров R8–R15 отличается от работы с AX/AH/AL? ⭐⭐
- Почему разные имена частей регистра не следует воспринимать как независимые регистры? ⭐⭐
- Какие идеи, изученные на примере Intel 8080, сохраняются при переходе к x86-64? ⭐⭐
- В чём принципиальное отличие учебной архитектуры Intel 8080 от x86-64 как реальной промышленной архитектуры? ⭐⭐
- Какую роль выполняет RIP в управлении выполнением программы? ⭐⭐
- Почему RIP обычно изменяется автоматически, но может быть изменён инструкциями перехода и вызова? ⭐⭐
- Как инструкции `call`, `ret`, `push` и `pop` изменяют RSP? ⭐⭐
- Для чего используется регистр RFLAGS? ⭐⭐
- Почему флаги считаются побочным результатом выполнения многих инструкций? ⭐⭐
- Чем различаются ZF, CF, SF и OF по смыслу? ⭐⭐
- Почему CF важен для беззнаковой арифметики, а OF — для знаковой? ⭐⭐⭐
- Как один и тот же результат арифметической операции может по-разному интерпретироваться через разные флаги? ⭐⭐⭐
- Почему условные переходы не «знают», какие числа сравнивались — знаковые или беззнаковые? ⭐⭐⭐
- Как инструкции `cmp` и `test` подготавливают флаги для последующих переходов? ⭐⭐

## Архитектура x86-64: базовые инструкции и арифметические операции

- Чем инструкция `mov rax, rbx` отличается от `mov rax, [rbx]`? ⭐⭐
- Почему квадратные скобки в Intel-синтаксисе принципиально меняют смысл операнда? ⭐⭐
- Какие варианты пересылки данных поддерживает инструкция `mov`? ⭐⭐
- Чем `lea` отличается от `mov`, несмотря на похожий синтаксис с квадратными скобками? ⭐⭐
- Почему `lea` часто используется как арифметическая инструкция? ⭐⭐
- Почему `lea eax, [edi + esi]` может быть предпочтительнее пары `mov eax, edi; add eax, esi`? ⭐⭐⭐
- Какие флаги изменяет `add`, и почему это может быть важно для последующего кода? ⭐⭐
- Почему `cmp a, b` фактически выполняет `a - b`, но не сохраняет результат? ⭐⭐
- Чем `test a, b` отличается от `and a, b`? ⭐⭐
- Почему `xor eax, eax` часто используется для обнуления регистра? ⭐⭐
- Почему многие особенности x86-64 объясняются историческим развитием, а не оптимальностью проектирования? ⭐⭐
- Как обратная совместимость повлияла на сложность современной системы команд x86-64? ⭐⭐
- Почему зависимости между соседними инструкциями могут замедлять выполнение программы на современном процессоре? ⭐⭐
- Почему при оптимизации важно учитывать не только количество инструкций, но и зависимости между ними? ⭐⭐⭐
- Почему инструкции сложения и вычитания в x86-64 проще в использовании, чем инструкция деления? ⭐⭐
- В каких регистрах должна находиться делимая величина перед выполнением `idiv` в 64-битном режиме? ⭐⭐
- Зачем перед знаковым делением используется инструкция `cqo`? ⭐⭐
- В каких регистрах содержится результат инструкции `idiv`? ⭐⭐
- Чем `cdq` отличается от `cqo` с точки зрения разрядности? ⭐⭐
- Почему деление считается одной из наиболее «дорогих» операций для процессора? ⭐⭐
- Как ассемблерный код вычисления среднего значения трёх чисел демонстрирует соглашения об использовании регистров? ⭐⭐
- Какие ошибки могут возникнуть при смешивании 32-битных и 64-битных арифметических операций? ⭐⭐⭐
- Почему доступ к памяти и деление часто являются более дорогими операциями, чем простая арифметика? ⭐⭐
- Чем latency отличается от throughput при анализе инструкций процессора? ⭐⭐
- Почему инструкция с большой latency не обязательно ограничивает throughput всей программы? ⭐⭐⭐

## Архитектура x86-64: переходы, циклы и управление потоком, управление памятью и адресация

- Чем безусловный переход `jmp` отличается от условных переходов? ⭐⭐
- Почему `je` и `jz` являются синонимами? ⭐⭐
- Почему для знаковых и беззнаковых сравнений используются разные инструкции перехода? ⭐⭐⭐
- Чем `jg` отличается от `ja` после одной и той же инструкции `cmp`? ⭐⭐⭐
- Почему `jb` и `jc` могут обозначать одну и ту же проверку флага? ⭐⭐
- Как на ассемблере реализуется цикл прохода по ASCIIZ-строке? ⭐⭐
- Почему проверка нулевого байта используется для определения конца строки в примере подсчёта символов? ⭐⭐
- Как инструкции `cmp`, `je`, `jne` и `jmp` совместно реализуют конструкцию `while` или `for`? ⭐⭐
- Почему непредсказуемые условные переходы могут снижать производительность программы? ⭐⭐⭐
- Как branchless-код может заменить условные переходы операциями над данными? ⭐⭐⭐
- Что такое branch prediction и зачем он нужен процессору при выполнении условных переходов? ⭐⭐
- Почему ошибка предсказания перехода приводит к сбросу уже начатого выполнения инструкций? ⭐⭐
- Что означает представление памяти как линейного адресного пространства? ⭐⭐
- Почему указатель в C можно рассматривать как число, интерпретируемое как адрес? ⭐⭐
- Чем значение регистра отличается от данных по адресу, который хранится в этом регистре? ⭐⭐
- Как размер регистра влияет на количество байтов, читаемых или записываемых инструкцией `mov`? ⭐⭐
- Что означает адресация вида `[base + index * scale + offset]`? ⭐⭐
- Какие значения может принимать множитель `scale` в x86-64 и почему именно такие? ⭐⭐
- Как адресация `[rdi + rsi*4]` связана с доступом к элементам массива? ⭐⭐
- Как адресация `[rdi + rdx*4 + 2]` может использоваться для доступа к полям структуры? ⭐⭐⭐
- Почему при обращении к памяти иногда явно указываются `BYTE PTR`, `WORD PTR`, `DWORD PTR` или `QWORD PTR`? ⭐⭐
- Почему прямые абсолютные адреса редко используются в современном прикладном коде? ⭐⭐
- Почему переход от 32-битной к 64-битной архитектуре стал необходимым с точки зрения адресации памяти? ⭐⭐
- Как процессор 80386 повлиял на современную модель памяти x86? ⭐⭐
- Почему чтение данных из памяти может быть намного медленнее операций над регистрами? ⭐⭐
- Как процессор может скрывать задержки доступа к памяти за счёт выполнения независимых инструкций? ⭐⭐⭐

## Архитектура x86-64: функции, стек и ABI, косвенные вызовы

- Что делает инструкция `call` помимо передачи управления функции? ⭐⭐
- Как инструкция `ret` узнаёт, куда нужно вернуть управление? ⭐⭐
- Почему стек является ключевым механизмом реализации вложенных вызовов функций? ⭐⭐
- Какие аргументы функции передаются через регистры в System V ABI? ⭐⭐
- Где возвращается целочисленный результат функции в System V ABI? ⭐⭐
- Чем caller-saved регистры отличаются от callee-saved? ⭐⭐
- Почему функция обязана восстановить callee-saved регистры, если она их использовала? ⭐⭐
- Почему вызывающая функция должна сама сохранять caller-saved регистры, если их значения ей нужны после вызова? ⭐⭐
- Зачем стек должен быть выровнен на границу 16 байт перед вызовом функции? ⭐⭐⭐
- Почему современные компиляторы иногда не используют RBP как указатель стекового кадра? ⭐⭐⭐
- Почему локальная переменная может быть помещена в стек, если в программе берётся её адрес? ⭐⭐
- Для чего перед вызовом вариативной функции вроде `printf` может обнуляться RAX? ⭐⭐⭐
- Что такое tail call optimization и почему компилятор может заменить `call` на `jmp`? ⭐⭐⭐
- Почему функция, которая сразу возвращает результат другой функции,
  является хорошим кандидатом на tail call optimization? ⭐⭐
- Почему x86-64 оказался успешнее IA-64 с точки зрения совместимости уже существующего программного обеспечения? ⭐⭐⭐
- В чём различие между подходами IA-64 и AMD64 к переходу на 64 бита? ⭐⭐⭐
- Почему функция в C может рассматриваться как адрес участка кода в памяти? ⭐⭐
- Чем прямой вызов `call f` отличается от косвенного вызова `call rax`? ⭐⭐
- Как указатели на функции позволяют реализовывать callback-механизмы? ⭐⭐
- Почему при вызове функции через указатель особенно важно соблюдать ABI? ⭐⭐
- Как функция `Apply`, принимающая указатель на функцию, может быть реализована через `call rax`? ⭐⭐
- Почему аргументы иногда приходится перекладывать между регистрами перед косвенным вызовом? ⭐⭐
- Почему значение аргумента может сохраняться в RBX перед вызовом функции через указатель? ⭐⭐
- Как `qsort` демонстрирует практическое использование указателей на функции? ⭐⭐
- Почему функция сравнения для `qsort` получает указатели `const void*`, а не значения элементов напрямую? ⭐⭐
- Как инструкции `setg`, `movzx` и `cmovl` позволяют построить сравнение без ветвлений? ⭐⭐⭐

## Архитектура x86-64: SIMD, SSE и AVX

- Что означает SIMD и какую проблему скалярной обработки он решает? ⭐⭐
- Почему обработка массивов по одному элементу плохо использует возможности современных процессоров? ⭐⭐
- Чем `addps xmm0, xmm1` отличается от обычного `add eax, ebx`? ⭐⭐
- Сколько 32-битных чисел `float` помещается в XMM-регистр? ⭐⭐
- Сколько 32-битных целых чисел помещается в YMM-регистр? ⭐⭐
- Почему SIMD-регистр сам по себе не хранит информацию о типе данных? ⭐⭐
- Как инструкция определяет, интерпретируются ли данные SIMD-регистра как `int`, `float` или `double`? ⭐⭐
- Чем `addss` отличается от `addps`? ⭐⭐
- Чем SSE отличается от AVX с точки зрения размера регистров и формата инструкций? ⭐⭐
- В чём преимущество трёхоперандного формата AVX перед двухоперандным форматом SSE? ⭐⭐⭐
- Как перекрываются XMM, YMM и ZMM-регистры? ⭐⭐
- Что происходит со значением в регистре YMM0 при записи в XMM0? ⭐⭐⭐
- Для чего используется инструкция `vzeroupper`? ⭐⭐⭐
- Почему SIMD-инструкции могут иметь высокую latency, но всё равно давать большой выигрыш в производительности? ⭐⭐⭐
- Как SIMD помогает уменьшить число выполняемых инструкций при обработке массивов? ⭐⭐
- Как SIMD, branchless-код и out-of-order execution совместно влияют на производительность современных программ? ⭐⭐⭐
- Почему компилятор при автоматической векторизации часто генерирует несколько вариантов цикла? ⭐⭐⭐
- Зачем перед SIMD-циклом компилятор проверяет размер массива? ⭐⭐
- Почему компилятор проверяет возможное пересечение областей памяти перед векторизацией? ⭐⭐⭐
- Почему после основного SIMD-цикла часто нужен код обработки «хвоста» массива? ⭐⭐
- Чем обработка четырёх `int` через XMM отличается от обработки восьми `int` через YMM? ⭐⭐
- Чем aligned SIMD-загрузки отличаются от unaligned-загрузок? ⭐⭐
- Почему выравнивание памяти важно для SIMD-инструкций? ⭐⭐
- Почему в System V ABI требуется выравание стека по 16-байтной границе? ⭐⭐⭐
- Почему автоматическая векторизация может сделать ассемблерный код длиннее, но ускорить выполнение программы? ⭐⭐⭐

## Архитектура x86-64: intrinsics, floating point и ABI

- Что такое SIMD intrinsics и чем они отличаются от ручного ассемблера? ⭐⭐
- Почему intrinsics можно считать компромиссом между C-кодом и ассемблером? ⭐⭐
- Какие типы данных вводятся для работы с SSE intrinsics? ⭐⭐
- Что делает функция `_mm_loadu_ps`? ⭐⭐
- Как `_mm_add_ps` обычно отображается на SIMD-инструкцию процессора? ⭐⭐
- Почему код с intrinsics становится зависимым от конкретной архитектуры процессора? ⭐⭐
- Когда ручное использование intrinsics может быть предпочтительнее автоматической векторизации компилятором? ⭐⭐⭐
- Какие риски возникают при написании SIMD-кода вручную через intrinsics? ⭐⭐⭐
- Почему при использовании intrinsics программист должен учитывать не только типы данных,
  но и особенности памяти и выравнивания? ⭐⭐⭐
- Почему современные floating point операции даже в scalar-варианте часто используют XMM-регистры? ⭐⭐
- Чем scalar floating point инструкции отличаются от packed floating point инструкций? ⭐⭐
- Чем `addss` отличается от `addps` с точки зрения количества обрабатываемых элементов? ⭐⭐
- Чем `addsd` отличается от `addpd`? ⭐⭐
- Через какие регистры передаются floating point аргументы в System V ABI? ⭐⭐
- В каком регистре возвращается значение типа `float` или `double`? ⭐⭐
- Почему целочисленные и floating point аргументы одной функции могут передаваться через разные группы регистров? ⭐⭐
- Как инструкция `cvtsi2ss` связывает целочисленную арифметику и floating point вычисления? ⭐⭐
- Почему перед `cvtsi2ss` компилятор может занулять XMM-регистр с помощью `pxor`? ⭐⭐⭐
- Почему все XMM-регистры в System V ABI считаются caller-saved? ⭐⭐
- Почему правило caller-saved для XMM-регистров распространяется также на YMM и ZMM? ⭐⭐⭐
- Почему SIMD-типы вроде `__m128` и `__m256` передаются через те же регистровые механизмы,
  что и floating point данные? ⭐⭐⭐
- Как использование intrinsics влияет на переносимость программы между процессорами с разной поддержкой SSE,
  AVX и AVX-512? ⭐⭐⭐
- Почему при смешивании scalar floating point кода и SIMD-кода важно понимать перекрытие
  XMM, YMM и ZMM-регистров? ⭐⭐⭐

# Программирование микроконтроллеров

## Программирование микроконтроллеров: встраиваемые системы и микроконтроллеры

- Чем встраиваемая система отличается от компьютера общего назначения? ⭐⭐
- Почему микроконтроллеры особенно часто используются во встраиваемых системах? ⭐⭐
- Какие преимущества даёт размещение CPU, памяти и периферии в одном чипе? ⭐⭐
- Какие ограничения накладывают малые объёмы SRAM и Flash на стиль embedded-программирования? ⭐⭐
- Почему embedded-программа обычно теснее связана с аппаратной архитектурой, чем desktop-программа? ⭐⭐
- Какие задачи обычно решает микроконтроллер при взаимодействии с физическим миром? ⭐
- Почему микроконтроллер можно рассматривать как систему на кристалле? ⭐⭐
- В чём различие между Flash, SRAM и EEPROM в микроконтроллере? ⭐⭐
- Почему во встраиваемых системах особенно важно понимать layout памяти программы? ⭐⭐
- Как отсутствие виртуальной памяти влияет на безопасность и надёжность embedded-программы? ⭐⭐⭐
- Почему embedded-программы часто работают без операционной системы? ⭐⭐
- Какие преимущества и недостатки даёт работа «ближе к железу»? ⭐⭐
- Чем cross-compilation отличается от обычной компиляции под компьютер разработчика? ⭐⭐
- Почему embedded-программа обычно запускается после включения питания и работает непрерывно? ⭐
- Почему функции внутри Super Loop должны быстро возвращать управление? ⭐⭐

## Программирование микроконтроллеров: архитектура AVR, организация памяти, система команд

- Какие особенности архитектуры AVR делают её удобной для обучения embedded-программированию? ⭐⭐
- Что означает, что AVR является 8-битной RISC-архитектурой? ⭐⭐
- В чём суть гарвардской архитектуры AVR? ⭐⭐
- Почему адрес 0x0000 в Program Space и адрес 0x0000 в Data Space не конфликтуют друг с другом? ⭐⭐⭐
- Какие преимущества даёт физическое разделение памяти программ и памяти данных? ⭐⭐
- Почему таблица векторов прерываний находится во Flash, а регистры периферии — в Data Space? ⭐⭐
- Как наличие 32 регистров общего назначения влияет на эффективность AVR-кода? ⭐⭐
- Какие пары регистров AVR используются как указатели и почему это важно? ⭐⭐
- Почему регистры X, Y и Z особенно полезны при работе с массивами и строками? ⭐⭐
- Как регистровый файл AVR отображается в адресное пространство данных? ⭐⭐
- Что означает memory-mapped I/O в контексте AVR? ⭐⭐
- Почему часть I/O-регистров имеет два адреса: I/O address и Data memory address? ⭐⭐⭐
- Как связаны I/O address и Data memory address для регистров AVR? ⭐⭐
- Почему инструкции `in` и `out` не всегда можно использовать для доступа к любому регистру периферии? ⭐⭐⭐
- Чем доступ к регистру через `in/out` отличается от доступа через `lds/sts`? ⭐⭐
- Почему Stack Pointer в AVR хранится в специальных I/O-регистрах? ⭐⭐
- Где располагается стек в ATmega328P и в какую сторону он растёт? ⭐⭐
- Почему startup code устанавливает Stack Pointer до вызова `main()`? ⭐⭐
- Зачем при компиляции AVR-программы указывать параметр `-mmcu=atmega328p`? ⭐⭐
- Что такое архитектура Super Loop? Какие преимущества имеет Super Loop в небольших embedded-системах? ⭐⭐

## Программирование микроконтроллеров: система команд AVR, стартовый код

- Какие группы инструкций AVR используются для передачи данных, арифметики, логики и управления потоком? ⭐
- Почему большинство арифметических операций AVR выполняется над регистрами, а не напрямую над памятью? ⭐⭐
- В каких случаях компилятор может заменить read-modify-write последовательность на инструкцию `sbi` или `cbi`? ⭐⭐⭐
- Почему операция `DDRB |= (1 << PB5)` может скомпилироваться в одну инструкцию `sbi`? ⭐⭐
- Почему операция с одним и тем же регистром в C
  может компилироваться в разные AVR-инструкции в зависимости от адреса регистра? ⭐⭐⭐
- Как операция `PORTB ^= (1 << PB5)` превращается в последовательность `in`, `ldi`, `eor`, `out`? ⭐⭐
- Почему дизассемблирование особенно полезно при разработке под микроконтроллеры? ⭐⭐
- Что можно узнать о программе, анализируя результат `avr-objdump`? ⭐⭐
- Почему бесконечный цикл в C может быть представлен инструкцией `rjmp` на саму себя? ⭐
- Как машинный код показывает, что interrupt — это аппаратный механизм, а не «магия языка C»? ⭐⭐⭐
- Почему embedded-программа фактически начинает выполнение не с `main()`? ⭐⭐
- Что такое reset vector и какую роль он играет после сброса микроконтроллера? ⭐⭐
- Какие действия обычно выполняет startup code перед вызовом `main()`? ⭐⭐
- Почему секция `.data` должна копироваться из Flash в SRAM? ⭐⭐⭐
- Почему секция `.bss` должна быть обнулена перед началом выполнения программы на C? ⭐⭐
- Почему регистр `r1` в AVR ABI обычно должен содержать ноль? ⭐⭐⭐
- Зачем startup code очищает регистр SREG? ⭐⭐
- Почему в embedded-программах `main()` обычно не должен завершаться? ⭐
- Что происходит, если `main()` всё же возвращает управление runtime-коду? ⭐⭐
- Как обработчик `__bad_interrupt` помогает обнаруживать или обрабатывать неожиданные прерывания? ⭐⭐
- Какие задачи выполняют `avr-gcc`, `avr-as`, `avr-ld`, `avr-objdump`, `avr-size` и `avrdude`? ⭐
- Как прерывания могут дополнять архитектуру Super Loop? ⭐⭐

## Программирование микроконтроллеров: аппаратные регистры, volatile и GPIO, таймеры

- Чем аппаратный регистр отличается от обычной переменной программы? ⭐⭐
- Почему аппаратные регистры должны объявляться как `volatile`? ⭐⭐
- Какие оптимизации компилятора могут нарушить работу программы без `volatile`? ⭐⭐
- Почему `volatile` запрещает кэширование значения регистра, но не делает операцию атомарной? ⭐⭐⭐
- Как чтение и запись аппаратного регистра могут иметь побочные эффекты? ⭐⭐
- Почему порядок записи в регистры периферии часто имеет принципиальное значение? ⭐⭐⭐
- Как GPIO позволяют микроконтроллеру взаимодействовать с внешним миром? ⭐
- Какие регистры обычно используются для управления GPIO в ATmega328P? ⭐⭐
- Чем отличаются роли регистров `DDRx`, `PORTx` и `PINx`? ⭐⭐
- Что означает установка бита в `DDRB` для соответствующего вывода порта B? ⭐
- Как с помощью `PORTx` управляется логический уровень вывода в режиме output? ⭐
- Почему запись единицы в `PORTx` при режиме input включает pull-up резистор? ⭐⭐
- Зачем нужен pull-up резистор при подключении кнопки к цифровому входу? ⭐⭐
- Почему неподключённый цифровой вход может иметь неопределённое состояние? ⭐⭐
- Почему GPIO нельзя напрямую использовать для управления мощной нагрузкой без дополнительных компонентов? ⭐⭐
- Почему результатом сборки embedded-программы обычно является прошивка, а не обычный исполняемый файл ОС? ⭐⭐
- Почему конечные автоматы удобны для описания поведения сложных устройств? ⭐⭐
- Чем аппаратный таймер отличается от программной задержки в цикле? ⭐⭐
- Почему таймер может работать независимо от CPU? ⭐⭐
- Какие задачи во встраиваемых системах решаются с помощью таймеров? ⭐
- Как связаны clock source, prescaler, counter и compare register? ⭐⭐
- Зачем нужен prescaler в аппаратном таймере? ⭐⭐
- Как рассчитать значение compare register
  для генерации события один раз в секунду при F_CPU = 16 MHz и prescaler = 1024? ⭐⭐
- Что происходит при событии compare match в режиме CTC? ⭐⭐
- Почему важно настроить режим таймера до записи некоторых параметров сравнения? ⭐⭐⭐
- Какие аппаратные действия может выполнить таймер при наступлении compare match? ⭐⭐
- Почему аппаратный таймер лучше подходит для точного времени, чем `_delay_ms()`? ⭐⭐
- Чем ELF-файл отличается от HEX-файла в процессе разработки под AVR? ⭐⭐
- Как состояние системы влияет на реакцию программы на одно и то же событие? ⭐⭐
- Почему длительную обработку события лучше выполнять в основном цикле, а не внутри ISR? ⭐⭐

## Программирование микроконтроллеров: прерывания и обработчики ISR, polling, sleep mode

- Что такое прерывание и чем оно отличается от обычного вызова функции? ⭐⭐
- Какие аппаратные события могут быть источниками прерываний в микроконтроллере? ⭐
- Что делает CPU при возникновении прерывания? ⭐⭐
- Почему ISR должна завершаться инструкцией `reti`, а не обычной `ret`? ⭐⭐⭐
- Как макрос `ISR(...)` связывает функцию C с interrupt vector? ⭐⭐
- Почему ISR имеет специальный пролог и эпилог на уровне машинного кода? ⭐⭐⭐
- Какие регистры и флаги должен сохранять обработчик прерывания? ⭐⭐
- Почему ISR может возникнуть практически в любой момент выполнения основной программы? ⭐⭐
- Почему обработчики прерываний должны быть короткими и предсказуемыми? ⭐⭐
- Что такое interrupt latency и из каких этапов она складывается? ⭐⭐⭐
- Какие последствия может иметь большая interrupt latency в real-time системе? ⭐⭐⭐
- Что такое jitter и почему он опасен для систем с жёсткими временными ограничениями? ⭐⭐⭐
- Что происходит с другими прерываниями, пока AVR выполняет ISR? ⭐⭐
- Что такое interrupt nesting и почему его обычно избегают на AVR? ⭐⭐⭐
- Почему выполнение `sei()` внутри ISR может усложнить программу? ⭐⭐⭐
- Почему для прошивки микроконтроллера часто используется формат Intel HEX? ⭐
- Чем polling отличается от обработки событий через interrupts? ⭐⭐
- Почему постоянный опрос флага в цикле неэффективен с точки зрения использования CPU? ⭐⭐
- В каких случаях polling может быть допустимым или даже удобным? ⭐⭐
- Почему при использовании прерываний CPU может большую часть времени находиться в sleep mode? ⭐⭐
- Как аппаратный таймер может пробудить CPU из режима сна? ⭐⭐
- Что делает инструкция `sleep` в AVR? ⭐⭐
- Почему перед выполнением `sleep` нужно настроить соответствующие регистры управления сном? ⭐⭐
- Как event-driven архитектура отличается от busy-wait подхода? ⭐⭐
- Почему sleep mode особенно важен для устройств с батарейным питанием? ⭐⭐
- Что происходит во время записи прошивки во Flash-память микроконтроллера? ⭐⭐
- Почему предсказуемость поведения является ключевым требованием embedded-систем? ⭐⭐

## Программирование микроконтроллеров: флаги, прерывания, критические секции, состояние гонки и атомарность

- Почему прерывание — это не само аппаратное событие, а реакция процессора на него? ⭐⭐
- Как связаны hardware event, interrupt flag, interrupt enable bit и global interrupt enable? ⭐⭐⭐
- Чем отличаются регистры флагов прерываний и регистры разрешения прерываний? ⭐⭐
- Почему установленный interrupt flag не всегда приводит к немедленному вызову ISR? ⭐⭐
- В каких ситуациях событие может быть отложено или потеряно? ⭐⭐⭐
- Для чего используются инструкции `cli` и `sei`? ⭐
- Почему временный запрет прерываний может использоваться как критическая секция? ⭐⭐
- Чем опасны слишком длинные критические секции? ⭐⭐⭐
- Почему правило embedded-разработки требует запрещать прерывания как можно реже и на минимальное время? ⭐⭐
- Как запрет прерываний влияет на interrupt latency? ⭐⭐
- Почему при работе с UART или таймерами длительный `cli()` может привести к ошибкам? ⭐⭐⭐
- Что такое состояние гонки в embedded-программе? ⭐⭐
- Почему общие данные между ISR и основной программой требуют особой осторожности? ⭐⭐
- Почему чтение 16-битной переменной на 8-битном AVR не является атомарной операцией? ⭐⭐⭐
- Как может возникнуть повреждённое значение при чтении `volatile uint16_t counter`? ⭐⭐⭐
- Почему `volatile` не защищает от race condition? ⭐⭐⭐
- Как временный запрет прерываний помогает безопасно прочитать 16-битную переменную? ⭐⭐
- Какие недостатки есть у защиты данных через `cli()` и `sei()`? ⭐⭐⭐
- Почему ISR часто только устанавливает флаг, а основная обработка выполняется в главном цикле? ⭐⭐
- Почему анализ размера программы особенно важен для ATmega328P? ⭐⭐
- Как сочетание Super Loop, ISR, флагов и конечных автоматов помогает строить надёжные embedded-программы? ⭐⭐⭐

## Программирование микроконтроллеров: UART, SPI

- Для чего используется UART во встраиваемых системах? ⭐
- Почему UART называется асинхронным интерфейсом? ⭐⭐
- Какие линии используются для передачи и приёма данных по UART? ⭐
- Почему обе стороны UART-соединения должны заранее договориться о скорости передачи? ⭐⭐
- Что означает формат UART 115200 8N1? ⭐⭐
- Как устроен UART-кадр при передаче одного байта? ⭐⭐
- Почему линия UART в состоянии покоя находится в логической единице? ⭐⭐
- Почему стартовый бит имеет значение логического нуля? ⭐⭐
- В каком порядке передаются биты данных в UART-кадре? ⭐⭐
- Какие ошибки могут возникнуть при несовпадении настроек UART у передатчика и приёмника? ⭐⭐
- Какие регистры ATmega328P используются для настройки скорости и формата UART? ⭐⭐
- Что означает флаг `UDRE0` и как он используется при передаче байта? ⭐⭐
- Что означает флаг `RXC0` и как он используется при приёме байта? ⭐⭐
- Почему программа не должна вручную формировать стартовые и стоповые биты при использовании аппаратного UART? ⭐⭐
- Как реализуется echo-программа с изменением регистра символов через UART? ⭐⭐
- Чем SPI отличается от UART с точки зрения синхронизации передачи данных? ⭐⭐
- Какие линии используются в SPI и какова роль каждой из них? ⭐
- Почему SPI называется полнодуплексным интерфейсом? ⭐⭐
- Что означает разделение устройств SPI на Master и Slave? ⭐⭐
- Почему сигнал SCK формируется ведущим устройством? ⭐⭐
- Для чего используется сигнал SS или CS? ⭐⭐
- Как несколько SPI-устройств могут использовать общие линии MOSI, MISO и SCK? ⭐⭐
- Почему для каждого SPI-slave обычно требуется отдельная линия CS? ⭐⭐
- Что происходит при передаче одного байта по SPI на уровне сдвиговых регистров? ⭐⭐⭐
- Почему даже «запись» по SPI одновременно является и чтением? ⭐⭐
- В каких случаях SPI предпочтительнее UART и I²C? ⭐⭐
- Почему embedded-разработчик должен понимать одновременно язык C, машинный код и устройство периферии? ⭐⭐⭐

## Программирование микроконтроллеров: I²C, ADC, PWM

- Зачем был нужен интерфейс I²C и какую проблему он решает? ⭐⭐
- Какие две линии используются в I²C? ⭐
- Чем I²C отличается от SPI при подключении большого количества устройств? ⭐⭐
- Как адресация устройств в I²C заменяет отдельные линии выбора CS? ⭐⭐
- Для чего в I²C используется бит R/W? ⭐
- Почему после каждого байта в I²C требуется ACK? ⭐⭐
- Что может означать отсутствие ACK от устройства? ⭐⭐
- Почему для шины I²C необходимы подтягивающие резисторы? ⭐⭐⭐
- Что означает open-drain режим работы линии? ⭐⭐⭐
- Почему устройство I²C может активно формировать ноль, но не формирует единицу? ⭐⭐⭐
- Как open-drain предотвращает электрический конфликт на общей линии? ⭐⭐⭐
- В каких случаях I²C предпочтительнее SPI? ⭐⭐
- Почему микроконтроллеру нужен ADC для взаимодействия с реальным миром? ⭐
- Какие физические величины можно измерять с помощью ADC? ⭐
- Как ADC преобразует входное напряжение в цифровое значение? ⭐⭐
- Как влияет опорное напряжение VREF на результат ADC-преобразования? ⭐⭐
- Какой диапазон значений возвращает 10-битный ADC ATmega328P? ⭐
- Какой результат примерно вернёт 10-битный ADC при VIN = 2V и VREF = 5V? ⭐⭐
- Какие регистры используются для настройки ADC в AVR? ⭐⭐
- Зачем ADC нужен делитель частоты? ⭐⭐
- Каков общий алгоритм чтения аналогового значения с выбранного канала? ⭐⭐
- Почему программа должна дождаться завершения ADC-преобразования перед чтением результата? ⭐⭐
- Что означает автоматический сброс бита ADSC после завершения преобразования? ⭐⭐
- Что такое PWM и почему он полезен при управлении нагрузкой? ⭐
- Почему цифровой вывод с помощью PWM может создавать аналогоподобный эффект? ⭐⭐
- Что такое коэффициент заполнения PWM-сигнала? ⭐
- Как изменение duty cycle влияет на среднюю мощность нагрузки? ⭐⭐
- Как PWM может использоваться для регулировки яркости светодиодов и скорости электродвигателей? ⭐
- Почему аппаратная генерация PWM с помощью таймера лучше программного переключения вывода? ⭐⭐
- Как связаны таймеры AVR и генерация PWM? ⭐⭐
- В чём состоит ключевая идея программирования микроконтроллеров как взаимодействия software и hardware? ⭐⭐
